# -*- coding: utf-8 -*-
"""09-20 批次日志分析追加器。

把第 22/23 位样本(15945479209739960765 / 7804999401726161950)的三表内容追加
进 user_issue_analyze.xlsx。行样式从上一既有行复制，不改动任何旧内容。
"""
import copy
import os
import shutil

import openpyxl

BASE = r"E:\online\酷彼智能\copiioai\patent\用户增长"
MAIN = os.path.join(BASE, "user_issue_analyze.xlsx")
VIEW = os.path.join(BASE, "user_issue_analyze - 视图.xlsx")

Q1_USER = "15945479209739960765"
Q1_PROCESS = """【用户场景】Google 登录新用户(guoxiang.315@gmail.com，auth:google 2026-09-20 01:35:17 成功，内部本地 uid 15945479209739960765，第 22 位样本)。来源渠道明确：01:34:11 的 page_view 的 ref 是 https://www.google.com/ —— **自然搜索进入**，不是邀请或社群引流，是首次出现「Google 自然搜索 → Google 登录 → 直接提问」这条完整冷启链路。从登录到首问 5 分钟(01:35:17 注册 → 01:40:17 提问)，中间无页面跳转，说明落点页面直接可用。首问是**按公开号 + 目标动作**的专家式提问：「CN114601976A全球专利族审查历史分析」——不是查这件专利是什么，而是指定要"全球族 + 审查历史"，属 IP 从业者/研发情报人员的作业语言，画像与既有样本 3044…(康复器械发明人)、1825…、7804… 同族(家族/审查差异诉求第 4 例)。

【提问时间线与逻辑链】
1. 01:40:17 query_stream 到达，新建会话；01:40:18 number_parse 把 CN114601976 判为 **publication** 型(CN1xxxxxxxx 核心号段)、置信 medium；01:40:30 search_rewrite **mode=structured** 生成 6 条中文阶梯 + 8 条英文阶梯（非语义改写，是结构化模板）。
2. **检索式退化**：中文阶梯的第 1 条是 `ti:(CN114601976A OR 114601976 OR 中国发明公布 OR 发明专利) AND ti:(专利族 OR 同族专利 OR 全球同族 OR 优先权 OR PCT申请) AND ti:(审查意见 OR 审查档案 OR 审查过程 OR 答复文件 OR 法律状态)`；英文第 1/2/3 条是 `(CN114601976A OR "CN 114601976 A" OR CN114601976) AND ("global patent family" OR …) AND ("prosecution history" OR …)`。即**把公开号与领域词一起写进检索式**——"全球专利族""审查历史"在专利文献里不是**主题**，没有专利权人会把它写进标题/摘要，这类查询在语料上只有"恰好题名含该号字面量"才可能命中。**这些带 AND 的连接式在本轮日志中无一执行**(执行侧只落了单号查询)。
3. 01:40:36 实际执行的是唯一有用的那一步：`baiten_search_map — query='CN114601976A' rows=None candidates=1 pages=1 total=1` → `number_resolve — merged=1`。即系统最终只取回了这一个公开号自己的记录。
4. 01:40:37 relevance scoring 1 条(scored=1，0.8s)；01:40:37 **family scoring probe — seeds=1 members=0 enabled=True**。
5. 01:40:40 `legal_status — candidates=['CN114601976A'] entries=1 covered=1`(法律状态已覆盖，**需求#18 正样本**)。
6. 01:40:50 [SMALL-LIST] 1 items 走忠实复现路径；01:40:57 落库 retrievable=1(需求#29 正样本)、面板下发 1 个号、助手回答 **assistant_len=1008**。

【关键机制核实(已在代码确认)】`family scoring probe` 的 members 取自 **候选池内**(`_unscored_family_members(pool, seeds, budget)`)——它做的是"把池内已有的同族成员提进打分预算"，**不做族数据获取**。`FAMILY_SCORE_ENABLED` 默认 1(日志 enabled=True 印证)。因此 seeds=1/members=0 的正确读法是：**池里没有该号的任何同族成员**，因为本轮根本没有去取族。系统对"全球专利族"这一诉求**没有数据能力**，只有单件著录 + 法律状态。

【系统响应核验】支持到的：单件检索闭环(号→著录→法律状态→面板落库)全程无错误；公开号解析国别与类型正确；法律状态覆盖(covered=1)。未支持到的：**用户唯一要的东西——全球族与审查历史——系统没有任何数据源**。系统提示词里的 Deep Analysis Task Boundary 要求此类诉求"调后台分析工具，或在答案里明说边界并给下一步"，模型照此应给出边界声明或"创建同族分析任务"入口；但从检索层看，**系统能给出的上限就是一句"做不到"**，这决定了本轮无论措辞多诚实都无法形成交付。"""

Q1_SUPPORT = """部分支持——但支持的恰好是用户没问的那部分。

已支持：
① 按公开号取件的闭环完整跑通。CN114601976A 解析为 publication 型(CN1xxxxxxxx 段)、置信 medium，佰腾按号直查命中 1 条，无无效重试、无跨源空打。
② 法律状态已覆盖：`legal_status entries=1 covered=1`——这是需求#18(法律状态问答)在本轮的实证，用户至少能拿到该号的当前法律状态。
③ 需求#29 自产号码可回读：`stored_conversation_patent_ids count=1 retrievable=1`，面板下发与回读一致。
④ 回答长度 1008 字，属中等篇幅，未见"未找到"式空答复。

未支持到（用户诉求的核心）：
① **全球专利族 = 零数据能力**。`family scoring probe — seeds=1 members=0` 的 members 只从**候选池内**捞(`_unscored_family_members`)，本轮池里只有该号自身 1 条，故恒为 0。这不是"族没匹配上"，是**根本没去取族**——系统没有 INPADOC/DOCDB/Espacenet 任一族的接入。
② **审查历史 = 零数据能力**。没有 Global Dossier / 审查卷宗 / 审查意见通知书的接入，OA、答复文件、驳回理由一概取不到。系统能给的"审查"相关信息只有法律状态事件流(需求#18 的时间线)，那不是审查历史。
③ **检索式对这类诉求是退化的**。生成侧把公开号与"全球专利族/审查历史"一起写成带 AND 的主题检索式(ti/ab 级)——这两个概念在专利文献里不是主题，没有申请人会把它写进标题；该形态在语料上近乎不可能命中。**本轮 6 条中文阶梯 + 8 条英文阶梯中，只有"单号"那一步命中，其余构造性无效。**
④ 因此用户拿到的是「号 + 著录 + 法律状态 + 一段 1008 字的答复」，而他问的是「族 + 审查历史」。这不是检索质量问题，是**能力边界**：无论用多好的检索式，没有族数据源就出不来族。

【与既有样本的关系】这是「家族/审查差异」类诉求的**第 4 例**(3044… → 1825… → 7804… → 本样本)。前 3 例登记在需求#21/#22；本样本的新增量是：它是**首例把诉求压缩在一个问题里、且用户来自 Google 自然搜索**的——说明这类专业诉求不是存量用户的行为，新客也直奔它而来。"""

Q1_ADVICE = """1. **把"族/审查"做成能力边界而不是检索任务**（最高优先，见需求#41）。当前生成侧对「XX号全球族审查历史」的反应是**构造带 AND 的主题检索式**(本轮 14 条阶梯里 13 条如此)，而这些检索式在语料上构造性无效——白白消耗检索预算且让模型误以为"搜过了"。正确形态是**识别为深分析类诉求**：直接告知"本系统可提供该号著录与法律状态，全球族与审查档案未接入"，并给出可执行入口(创建同族分析任务 / 上传文件)，不再生成主题检索式。
2. **接入族数据源**（需求#41 的实体部分）。用户的诉求是刚性的(4 例)，且 `family scoring` 机制**已经预留了位置**——它现在只从池内捞成员，一旦族数据能进池(或另开一条族通道)，这套打分/排序链路无需重写。建议优先评估 INPADOC/DOCDB 的可得性，而不是继续用检索逼近。
3. **检索式生成加"概念可检索性"判据**（需求#42）。一批阶梯里 13/14 构造性无效，说明生成侧没有区分「技术主题词」(可检索)与「文献管理概念」(不可检索：专利族、审查历史、法律状态、同族、优先权)。建议在生成前对这类概念做标注，含它们的连接式不再生成，改为触发能力声明。**判据要通用**(概念清单可配置)，不得固化本轮词汇。
4. **新客渠道信号值得追踪**。本样本是首例「Google 自然搜索 → Google 登录 → 专家式提问」，ref 明确记为 google.com。建议在 analytics 里把首次 page_view 的 ref 与后续提问类型关联统计——如果自然搜索带来的多是这类专业诉求，产品定位与落地页文案可以据此调整(当前落地页偏向泛检索)。"""

Q2_USER = "7804999401726161950"
Q2_PROCESS = """【用户场景】既有用户(内部 uid 7804999401726161950，**非新注册**)，2026-09-20 03:20 的会话。渠道线索充分：03:20:11 该用户先落在一个**结果面板 URL**上——`/app/results/?set=4eaa781d3eb142528209b28bfb1f0319-json&session_id=sess_ffd5d994e04d`，而它的 **ref 是另一个结果面板**(`set=ee714c078012439dba1d3dbc01cb5f0e…&session_id=sess_2fd1878aa45d`)，两者的 session_id 不同。随后同一秒落回 `/`，两次 auth:refresh，12 秒后发起提问。**读法**：这是一位在**多个结果面板之间来回切换**的用户，从上一轮的面板跳到新面板再回到首页提问——说明他已经在用系统的产出物做实际工作(面板 = 候选清单)，且跨会话查看。同一 uid 当日另有 04:19、06:10 两次 auth:refresh，全天 3 次回访，属留存用户。

【提问时间线与逻辑链】
1. 03:20:23 query_stream，正文**只有一个专利号**：「CN205762951U」，无任何自然语言问句、无动词、无问号。
2. 03:20:24 `number_source route — source=cn via ['CN205762951U']`；`number_parse — id_type=utility 置信 high`——中国实用新型授权公告(CN 前缀 + kind U)，解析正确且置信度最高档。
3. 03:20:35 `baiten_search_map — query='CN205762951U' rows=None candidates=1 pages=1 total=1` → `number_resolve merged=1`，**11 秒完成取件**。
4. 03:20:36 relevance scoring 1 条(0.8s)、family scoring probe seeds=1 members=0(enabled=True)。
5. 03:20:38 [SMALL-LIST] 1 items；03:20:44 落库 retrievable=1(需求#29 正样本)、下发面板、助手回答 **assistant_len=321**(本系列已记录样本中最短之一)。
6. **03:21:28 用户发起下载**：`Baiten download requested: CN205762951U (20161207)`——距答复仅 44 秒，且带上了公开日。日志无后续失败记录。

【系统响应核验】支持到的：裸号码提问的完整闭环(国别/类型识别 → 按号取件 → 著录 → 面板落库 → 下载入口可用)，全程无错误、无 404、无跨源空打；utility 类型识别正确(此前样本多见 publication/发明，utility 是首次)。值得注意的**次生信号**：用户拿到 321 字的答复后立刻要原文——本次会话的 1 条记录是实用新型，其可展示信息量本就单薄(公开号/题名/申请人/公开日/法律状态)，**交付物与用户的实际需求(要文件)之间有落差**，用户自行补上了这一步。这不是失败，但暴露了"按号查询默认交付摘要、而非默认给可取件入口"的习惯偏差。"""

Q2_SUPPORT = """支持良好——裸号码提问被完整承接，且用户自行完成了取件。

已支持：
① **类型识别**：CN205762951U 判为 utility(实用新型授权公告)且置信 high——`number_source route — source=cn via [...]` 走中国源，未误投美方接口。这是需求#6(号码标准化与号码-国家-工具匹配)与需求#24(裸号解析消歧)的又一个正样本。
② **取件闭环**：11 秒内 `baiten_search_map candidates=1 → number_resolve merged=1`，无重试、无空打。
③ 需求#19/#29：面板落库 retrievable=1，号码可回读。
④ **下载通路可用**：03:21:28 `Baiten download requested: CN205762951U (20161207)`——用户点击下载后，下载服务收到了请求并带上了公开日，日志中无失败与报错。
⑤ 全程零错误事件：本批 3 次提问、4 条 outbound_http(auth)，**无一条 404/403/超时**。与 09-19 批次(美方 37 请求仅 9 次 200)形成鲜明对照——纯 CN 号查询路径是健康的。

未支持到／有落差：
① 答复仅 **321 字**。对一件实用新型而言，系统手中确实只有公开号/题名/申请人/公开日/法律状态这几项，答复短是**诚实**的结果，不是偷懒；但用户 44 秒后就要下载原文，说明他的目标从一开始就是"拿到这份文献"，而系统默认交付的是"这份文献的摘要信息"。
② 同轮 `family scoring probe seeds=1 members=0`——单号查询同样没有族数据(与样本 22 同因)，但本用户未提族诉求，不构成缺口。
"""

Q2_ADVICE = """1. **按号查询默认给"取件入口"而非只给摘要**（见需求#43）。本用户 321 字答复后 44 秒即自行点击下载——**用户用行动补上了系统该主动给的那一步**。建议：单号查询的回答里，只要该号在当前数据源有可下载文献，就把"下载原文"作为默认交付项之一(而非等用户在面板里自己找)。判据通用：看该号是否有可取件记录，不针对任何具体专利。
2. **短答复不等于差答复，但要给出"还能做什么"**。321 字对实用新型是正常的信息量；问题在于答复没有告诉用户"这份是实用新型，可下载全文、可查同族(若接入)、可查法律状态时间线"。建议在按号查询的答复模板里固定一段"可继续的动作"清单——把能力的**存在性**告知用户，这也是需求#41 边界声明的正向版本。
3. **多面板切换行为值得埋点**。本用户带着**另一个 session 的结果面板**作为 ref 跳进新面板，说明他在对照两份候选清单。当前 analytics 只能看到 page_view 与 ref，看不到"用户在同一份清单上停留多久、点了哪些行"。建议在结果面板加轻量埋点(打开/下载/切换)，把"面板是可用产出物"这一判断从推断变成数据——这直接关系到需求#8(批量导出)的价值论证。"""

# ── 用户运营分析 ────────────────────────────────────────────────────────────
OPS_ROWS = [
    (
        "新增样本11(09-20 01:40,第22位样本-Google自然搜索+族/审查诉求)",
        "第 22 位样本（Google 登录 guoxiang.315@gmail.com，uid 15945479209739960765）。"
        "**渠道首次出现完整自然搜索链路**：01:34:11 page_view 的 ref=https://www.google.com/ "
        "→ 01:35:17 auth:google 注册 → 01:40:17 首问，登录到提问 5 分钟、中间无跳转。"
        "此前样本的进入方式为直接访问/结果页跳转，本样本证明产品能被自然搜索命中并直接承接。"
        "首问「CN114601976A全球专利族审查历史分析」是**专家式作业语言**(指定目标动作而非查询物件)，"
        "画像为 IP 从业者/研发情报人员，属「家族/审查差异」诉求第 4 例。"
        "**结果：系统给出了该号的著录与法律状态(covered=1、retrievable=1)，"
        "但用户唯一要的「全球族 + 审查历史」没有任何数据源**——"
        "family scoring probe 显示 seeds=1 members=0，而该机制只从候选池内捞成员，本轮池里只有该号自身。",
    ),
    (
        "新增样本11-检索式退化的量化",
        "本轮生成侧产出 **6 条中文阶梯 + 8 条英文阶梯**，其中 13 条把公开号与"
        "「专利族/审查历史」写成带 AND 的主题检索式（ti/ab 级），例如 "
        "`(CN114601976A OR …) AND (\"global patent family\" OR …) AND (\"prosecution history\" OR …)`。"
        "**这两个概念在专利文献里不是主题**——没有申请人会把「全球专利族」写进标题，"
        "因此这些检索式在语料上构造性无效。"
        "实际本轮只执行了其中的单号查询一条（baiten_search_map query='CN114601976A' → total=1），"
        "即 14 条阶梯里 13 条是无效构造。这不是个别现象：09-19 批次同样出现"
        "「微环谐振器/冗余备份」这类真主题词与「检索式/检索域」这类元概念混写。"
        "**改进方向是给生成侧加「概念可检索性」判据**（需求#42），而非继续调检索式。",
    ),
    (
        "新增样本12(09-20 03:20,第23位样本-裸号码+多面板回访用户)",
        "第 23 位样本（uid 7804999401726161950，**既有用户非新注册**）。"
        "最强信号是**进入路径**：03:20:11 他落在结果面板 `set=4eaa781d…&session_id=sess_ffd5d994e04d`，"
        "**ref 指向另一个结果面板** `set=ee714c07…&session_id=sess_2fd1878aa45d`——"
        "两个 session_id 不同，说明他在**跨会话对照两份候选清单**，"
        "面板对他已是实际产出物而非一次性展示。随后落回首页、12 秒后提问「CN205762951U」"
        "（**纯号码、无问句**），11 秒完成取件，答复 321 字，**44 秒后发起下载**。"
        "当日另有 04:19、06:10 两次 auth:refresh，全天 3 次回访。"
        "**结论：这是一条健康的留存路径**——按号查询全程零错误、utility 类型识别正确、"
        "下载通路可用；用户拿到摘要后自己补上了「要原文」这一步。",
    ),
    (
        "本批数据健康度(与前批对照)",
        "本批 3 次提问、4 条 outbound_http 全部为 auth 且 status=200 —— "
        "**无一条 404/403/429/超时**。与前批(09-19：USPTO 37 请求仅 9 次 200、"
        "死件占页 137 条、6 次空调用)形成对照：本批全是**纯 CN 号查询路径**，"
        "而这条路径是健康的。另注意 05:02:59 有一条匿名 page_view 的 path 带 "
        "`phpinfo=1` 与 base64 形态包裹的 `rest_route=/newfold-data/v1/verify/…` —— "
        "**这是扫描器指纹，不是用户行为**（与 09-19 批次的同类记录一致），"
        "统计用户量时应排除这类 vid。",
    ),
]

# ── 需求列表 ────────────────────────────────────────────────────────────────
REQ_ROWS = [
    (
        "41",
        "全球同族与审查档案的能力边界先行（识别「全球族/审查历史/OA 卷宗」类诉求为深分析类，"
        "直接声明能力边界并给可执行入口，不再生成主题检索式去逼近；同步评估 INPADOC/DOCDB 或 "
        "Global Dossier 类数据源接入——现有 family scoring 机制只从候选池内捞成员，"
        "族数据一旦能进池即复用，无需重写打分链路）",
        "1/25:15945479209739960765(2026-09-20 问「CN114601976A全球专利族审查历史分析」，"
        "family scoring probe seeds=1 members=0，系统仅有该号著录与法律状态)；"
        "家族/审查差异诉求第 4 例(3044…→1825…→7804…→本样本)",
        "高",
        "高",
        "高",
        "P0",
    ),
    (
        "42",
        "检索式生成的「概念可检索性」判据（区分技术主题词与文献管理概念——专利族/同族/优先权/"
        "审查历史/法律状态/OA 卷宗/检索式等元概念不可作为 ti/ab 主题词参与 AND 连接；"
        "含这类概念的连接式不再生成，改为触发能力声明或改用单号/申请人等可检索键；"
        "概念清单可配置、不固化任何领域词）",
        "1/25:15945479209739960765(2026-09-20 生成 14 条阶梯，13 条含「全球专利族/审查历史」"
        "且为构造性无效的主题连接式，实际仅单号查询一条被执行)",
        "中",
        "高",
        "中",
        "P1",
    ),
    (
        "43",
        "按号查询默认交付取件入口（单号查询的答复里，只要该号在当前数据源有可下载文献，"
        "就把「下载原文」列为默认交付项并给出「还能做什么」的固定清单——可下载/可查法律状态/"
        "可查同族(接入后)；判据看该号是否有可取件记录，不针对具体专利）",
        "1/25:7804999401726161950(2026-09-20 纯号码提问得 321 字摘要，44 秒后自行点击下载 "
        "Baiten download requested: CN205762951U)",
        "中",
        "中",
        "中",
        "P2",
    ),
]


def _clone_row_style(ws, src_row: int, dst_row: int) -> None:
    for c in range(1, ws.max_column + 1):
        s = ws.cell(src_row, c)
        d = ws.cell(dst_row, c)
        if s.has_style:
            d._style = copy.copy(s._style)


def main() -> None:
    shutil.copy2(MAIN, os.path.join(BASE, "user_issue_analyze - bak20260920b.xlsx"))
    wb = openpyxl.load_workbook(MAIN)

    ws = wb["用户问题分析"]
    for user, process, support, advice in (
            (Q1_USER, Q1_PROCESS, Q1_SUPPORT, Q1_ADVICE),
            (Q2_USER, Q2_PROCESS, Q2_SUPPORT, Q2_ADVICE)):
        r = ws.max_row + 1
        _clone_row_style(ws, ws.max_row, r)
        for c, v in enumerate([user, process, support, advice], 1):
            ws.cell(r, c, v)
        ws.cell(r, 1).alignment = copy.copy(ws.cell(r - 1, 1).alignment)
        print("用户问题分析 +1 → R%d" % r)

    ws = wb["用户运营分析"]
    for dim, concl in OPS_ROWS:
        r = ws.max_row + 1
        _clone_row_style(ws, ws.max_row, r)
        ws.cell(r, 1, dim)
        ws.cell(r, 2, concl)
    print("用户运营分析 +%d" % len(OPS_ROWS))

    ws = wb["需求列表"]
    for seq, need, ev, urg, imp, val, pri in REQ_ROWS:
        r = ws.max_row + 1
        _clone_row_style(ws, ws.max_row, r)
        for c, v in enumerate([seq, need, ev, urg, imp, val, pri], 1):
            ws.cell(r, c, v)
    print("需求列表 +%d → R%d" % (len(REQ_ROWS), ws.max_row))

    wb.save(MAIN)
    shutil.copy2(MAIN, VIEW)
    print("saved + synced view")


if __name__ == "__main__":
    main()
