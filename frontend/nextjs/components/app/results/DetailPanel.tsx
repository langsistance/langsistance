'use client'

/**
 * Placeholder detail panel — right column of the results page.
 * Task 14 replaces this with the row-detail / tabbed implementation.
 */
export default function DetailPanel(props: any) {
  return <div className="results-detail-placeholder">Detail panel placeholder — Task 14</div>
}
