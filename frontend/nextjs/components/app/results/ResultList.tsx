'use client'

/**
 * Placeholder result list — left (middle) column of the results page.
 * Task 13 replaces this with the full column/table implementation.
 */
export default function ResultList(props: any) {
  return <div className="results-list-placeholder">Result list placeholder — Task 13</div>
}
