# 小程序 M3 收尾包 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让小程序支持文件上传（PDF/DOCX/XML 单文件）、长任务进度、结果下载（CSV/XLSX 工件与 docx/pdf 报告）、转发分享与隐私政策，达到可提审状态；同时修掉长任务端点的越权。

**Architecture:** 后端沿用 `api_routes/session.py` 那轮的归属校验模式（`verify_firebase_token` + `WHERE ... AND user_id = %s`，失败一律 404）。前端新增四个 service 模块（upload / longTask / download / privacy）与三个展示组件，全部由 `pages/chat/index.tsx` 持有状态与网络调用——沿用形态一已确立的「组件展示、页面持状态」分工。

**Tech Stack:** Taro 3 + React（小程序端）、FastAPI + pytest（后端）、`wx.uploadFile` / `wx.downloadFile` / `FileSystemManager` / `wx.openDocument` / `wx.shareFileMessage`（平台能力）

**Spec:** `docs/superpowers/specs/2026-09-11-weapp-m3-design.md`

## Global Constraints

以下每条都来自 spec，**每个任务都隐含包含**：

- **`wx.uploadFile` 只能单文件**。`files` 数组参数是 uni-app 扩展，微信原生没有；`filePath` 传数组直接报错。不要尝试复刻 web 的多文件上传。
- **`wx.uploadFile` 没有 `onChunkReceived`**（那是 `RequestTask` 的方法）。上传分支不要写流式解析——读完整 `res.data` 再一次解析。
- **`wx.openDocument` 的 `fileType` 白名单**：`doc` / `docx` / `xls` / `xlsx` / `ppt` / `pptx` / `pdf`。**没有 csv**。
- **小程序没有 `Blob` / `URL.createObjectURL` / `<a download>`**。web 的下载实现（`frontend/nextjs/components/app/MarkdownMessage.tsx:100-110`、`:225-237`）整体不可移植。
- **归属校验失败一律返回 404**，不返回 403——不暴露「存在但不属于你」。
- **`wx.chooseMessageFile` 是隐私接口**：未在微信公众平台《小程序用户隐私保护指引》中声明「收集你选中的文件」，该接口**直接被禁用**。授权顺序必须是**先授权、再选文件**。
- **SCSS 里的 `px` 会被 Taro 编译成 `rpx`；内联 style 不经过编译管线，必须写 `rpx`**。两处写同一个数字实际渲染相差一倍。
- **后端测试必须先设环境变量**，否则 import 期就炸：
  ```bash
  cd E:/online/workspace/copiioai/langsistance && \
    REDIS_HOST=127.0.0.1 REDIS_PORT=6379 PYTHONUTF8=1 \
    python -m pytest tests/test_long_task_api.py -v
  ```
- **前端验证口径**（小程序无测试框架）：`npm run tsc` + `npm run build:weapp` 双绿，加开发者工具/真机人工验证。
- **开发者工具三条纪律**：导入路径固定 `frontend/weapp/dist` 不要切换（缓存按路径做键）；`compileHotReLoad` 已关，改后需**完全重启**工具；判断"代码跑没跑"优先在屏幕上画标记，其次 `console.error`，最后才 `console.log`（会被过滤）。

---

### Task 1: 后端 —— report 端点鉴权与归属校验

**Files:**
- Modify: `api_routes/long_task.py`（新增 `_task_owned_by` helper；改 `download_report`）
- Test: `tests/test_long_task_api.py`

**Interfaces:**
- Consumes: `sources.user.passport.verify_firebase_token`（`api_routes/long_task.py:24` 已导入）、`sources.knowledge.knowledge.get_db_connection`
- Produces: `_task_owned_by(task_id: str, user_id: int) -> bool` —— 供 Task 2 复用

**背景**：`GET /long_task/{task_id}/report` 当前**完全没有鉴权**（`api_routes/long_task.py:633-668` 只收 `task_id` + `format`），任何人不带凭证就能下载他人报告。`task_id` 形如 `lt_` + `uuid4().hex[:12]`（`api_routes/core.py:984`）。

**注意**：`tests/test_long_task_api.py` 现有的 `test_get_report_not_found`（`:68-76`）与 `test_get_report_success`（`:79-88`）**没有打 auth、也没 patch `verify_firebase_token`**——因为该端点现在没鉴权。加鉴权会让这两个测试失败，必须一并改。

- [ ] **Step 1: 写失败测试 —— 无凭证下载返回 401**

在 `tests/test_long_task_api.py` 末尾追加：

```python
# ── 越权修复: report 端点鉴权与归属校验 ──

def test_get_report_requires_auth(client):
    """无 Authorization 头 → 401（修复前该端点完全无鉴权）。"""
    response = client.get("/long_task/lt_001/report?format=pdf")
    assert response.status_code == 401


def test_get_report_other_users_task_returns_404(client):
    """他人任务 → 404（不是 403，不暴露"存在但不属于你"）。"""
    with patch('api_routes.long_task.verify_firebase_token') as mock_auth, \
         patch('api_routes.long_task._task_owned_by') as mock_owned:
        mock_auth.return_value = {"uid": "12345"}
        mock_owned.return_value = False
        response = client.get("/long_task/lt_other/report?format=pdf")
        assert response.status_code == 404


def test_get_report_ownership_check_uses_token_uid(client):
    """归属查询必须用 token 里的 uid，且带上 task_id —— 断言实际绑定参数，
    不只断言状态码（否则参数传错也能过）。"""
    with patch('api_routes.long_task.verify_firebase_token') as mock_auth, \
         patch('api_routes.long_task._task_owned_by') as mock_owned:
        mock_auth.return_value = {"uid": "12345"}
        mock_owned.return_value = True
        with patch('api_routes.long_task.create_storage') as mock_create:
            mock_storage = MagicMock()
            mock_storage.get = AsyncMock(return_value=b"fake pdf")
            mock_create.return_value = mock_storage
            response = client.get("/long_task/lt_mine/report?format=pdf")

    assert response.status_code == 200
    mock_owned.assert_called_once_with("lt_mine", 12345)
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd E:/online/workspace/copiioai/langsistance && \
  REDIS_HOST=127.0.0.1 REDIS_PORT=6379 PYTHONUTF8=1 \
  python -m pytest tests/test_long_task_api.py -k "report_requires_auth or other_users_task or ownership_check" -v
```

预期：`test_get_report_requires_auth` FAIL（拿到 404 而非 401，因为报告不存在）；另两个 FAIL（`_task_owned_by` 尚不存在，`mock.patch` 报 `AttributeError`）。

- [ ] **Step 3: 加 `_task_owned_by` helper**

在 `api_routes/long_task.py` 的 `_lookup_task_by_query_id_mysql` 之后（约 `:90`）插入：

```python
def _task_owned_by(task_id: str, user_id: int) -> bool:
    """True when *task_id* belongs to *user_id*.

    安全闸口，失败时**保守拒绝**（返回 False → 调用方给 404）：DB 故障时
    宁可拒绝合法请求，也不能放行越权读取。
    """
    if not task_id:
        return False
    try:
        from sources.knowledge.knowledge import get_db_connection
        conn = get_db_connection()
        try:
            with conn.cursor() as cur:
                cur.execute(
                    "SELECT 1 FROM long_tasks WHERE task_id = %s AND user_id = %s",
                    (task_id, user_id),
                )
                return cur.fetchone() is not None
        finally:
            conn.close()
    except Exception as e:
        return False
```

- [ ] **Step 4: 改 `download_report` 加鉴权与归属校验**

把 `api_routes/long_task.py:633-637` 的签名与开头：

```python
    @router.get("/long_task/{task_id}/report")
    async def download_report(task_id: str, format: str = Query(..., pattern="^(pdf|docx)$"), http_request: Request = None):
        """Download a completed report file for a task."""
        logger.info(f"Report download for task: {task_id}, format: {format}")
        storage = create_storage(get_storage_config())
```

改成：

```python
    @router.get("/long_task/{task_id}/report")
    async def download_report(task_id: str, format: str = Query(..., pattern="^(pdf|docx)$"), http_request: Request = None):
        """Download a completed report file for a task."""
        auth_header = http_request.headers.get("Authorization")
        user = verify_firebase_token(auth_header)
        user_id = int(user['uid'])
        if not _task_owned_by(task_id, user_id):
            # 404 而非 403 —— 不暴露「存在但不属于你」
            raise HTTPException(status_code=404, detail="Report not found")
        logger.info(f"Report download for task: {task_id}, format: {format}")
        storage = create_storage(get_storage_config())
```

- [ ] **Step 5: 修两个既有测试**

`tests/test_long_task_api.py:68-88` 的两个测试需要补 auth 与归属 mock。整体替换为：

```python
def test_get_report_not_found(client):
    """GET report for unknown task returns 404."""
    with patch('api_routes.long_task.verify_firebase_token') as mock_auth, \
         patch('api_routes.long_task._task_owned_by') as mock_owned, \
         patch('api_routes.long_task.create_storage') as mock_create:
        mock_auth.return_value = {"uid": "1"}
        mock_owned.return_value = True
        mock_storage = MagicMock()
        mock_storage.get = AsyncMock(side_effect=FileNotFoundError("no file"))
        mock_create.return_value = mock_storage

        response = client.get("/long_task/lt_nonexistent/report?format=pdf")
        assert response.status_code == 404


def test_get_report_success(client):
    """GET report for completed task returns file."""
    with patch('api_routes.long_task.verify_firebase_token') as mock_auth, \
         patch('api_routes.long_task._task_owned_by') as mock_owned, \
         patch('api_routes.long_task.create_storage') as mock_create:
        mock_auth.return_value = {"uid": "1"}
        mock_owned.return_value = True
        mock_storage = MagicMock()
        mock_storage.get = AsyncMock(return_value=b"fake pdf content")
        mock_create.return_value = mock_storage

        response = client.get("/long_task/lt_001/report?format=pdf")
        assert response.status_code == 200
        assert response.headers['content-type'] == 'application/pdf'
```

- [ ] **Step 6: 跑整个测试文件确认全绿**

```bash
cd E:/online/workspace/copiioai/langsistance && \
  REDIS_HOST=127.0.0.1 REDIS_PORT=6379 PYTHONUTF8=1 \
  python -m pytest tests/test_long_task_api.py -v
```

预期：全部 PASS（原有 6 个 + 新增 3 个）。

- [ ] **Step 7: 提交**

```bash
cd E:/online/workspace/copiioai/langsistance && \
  git add api_routes/long_task.py tests/test_long_task_api.py && \
  git commit -m "fix(long_task): report 端点补鉴权与归属校验——此前任何人可下载他人报告"
```

---

### Task 2: 后端 —— status 与 batch_status 归属校验

**Files:**
- Modify: `api_routes/long_task.py`（`task_status` `:552-559`、`batch_task_status` `:561-575`）
- Test: `tests/test_long_task_api.py`

**Interfaces:**
- Consumes: `_task_owned_by`（Task 1 产出）
- Produces: 无新导出

**背景**：这两个端点有 `verify_firebase_token` 但**不校验归属**，任何有效用户可轮询任意 `task_id`。与刚修完的 session IDOR 同类。

- [ ] **Step 1: 写失败测试**

追加到 `tests/test_long_task_api.py`：

```python
def test_status_other_users_task_returns_404(client):
    """他人任务的状态查询 → 404。"""
    with patch('api_routes.long_task.verify_firebase_token') as mock_auth, \
         patch('api_routes.long_task._task_owned_by') as mock_owned:
        mock_auth.return_value = {"uid": "12345"}
        mock_owned.return_value = False
        response = client.get("/long_task/lt_other/status")
        assert response.status_code == 404


def test_batch_status_filters_to_owned_only(client):
    """批量查询只返回本人的任务，不报错（避免被用来枚举 task_id 存在性）。"""
    with patch('api_routes.long_task.verify_firebase_token') as mock_auth, \
         patch('api_routes.long_task._task_owned_by') as mock_owned, \
         patch('api_routes.long_task.get_task_status') as mock_get:
        mock_auth.return_value = {"uid": "12345"}
        mock_owned.side_effect = lambda tid, uid: tid == "lt_mine"
        mock_get.return_value = {'task_id': 'lt_mine', 'status': 'running'}

        response = client.post("/long_task/batch_status",
                               json={"task_ids": ["lt_mine", "lt_other"]})

    assert response.status_code == 200
    statuses = response.json()["statuses"]
    assert "lt_mine" in statuses
    assert "lt_other" not in statuses
    # 只对本人任务查了状态
    mock_get.assert_called_once_with("lt_mine")


def test_batch_status_rejects_non_list_task_ids(client):
    """task_ids 不是数组时安全降级为空结果，不抛异常。"""
    with patch('api_routes.long_task.verify_firebase_token') as mock_auth:
        mock_auth.return_value = {"uid": "12345"}
        response = client.post("/long_task/batch_status", json={"task_ids": "lt_x"})
    assert response.status_code == 200
    assert response.json()["statuses"] == {}
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd E:/online/workspace/copiioai/langsistance && \
  REDIS_HOST=127.0.0.1 REDIS_PORT=6379 PYTHONUTF8=1 \
  python -m pytest tests/test_long_task_api.py -k "other_users_task or filters_to_owned or non_list_task_ids" -v
```

预期：前两个 FAIL（拿到 200）；第三个 PASS（现有代码已有 `isinstance` 兜底）。

- [ ] **Step 3: 改 `task_status`**

把 `api_routes/long_task.py:552-559` 整体替换为：

```python
    @router.get("/long_task/{task_id}/status")
    async def task_status(task_id: str, http_request: Request):
        """Poll the current status of a long-running task."""
        auth_header = http_request.headers.get("Authorization")
        user = verify_firebase_token(auth_header)
        user_id = int(user['uid'])
        if not _task_owned_by(task_id, user_id):
            raise HTTPException(status_code=404, detail="Task not found")
        logger.info(f"Status poll for task: {task_id}")
        status = get_task_status(task_id)
        return {"success": True, **status}
```

- [ ] **Step 4: 改 `batch_task_status`**

把 `api_routes/long_task.py:561-575` 整体替换为：

```python
    @router.post("/long_task/batch_status")
    async def batch_task_status(http_request: Request):
        """Poll status for multiple long-running tasks in one request."""
        auth_header = http_request.headers.get("Authorization")
        user = verify_firebase_token(auth_header)
        user_id = int(user['uid'])
        body = await http_request.json()
        task_ids = body.get("task_ids", []) or []
        if not isinstance(task_ids, list):
            task_ids = []
        # Cap at 20 to prevent abuse
        task_ids = task_ids[:20]
        statuses = {}
        for tid in task_ids:
            # 非本人的任务直接略过（不报错、不返回 unknown）——
            # 报错或返回 unknown 都会变成 task_id 存在性的探针
            if not _task_owned_by(tid, user_id):
                continue
            statuses[tid] = get_task_status(tid)
        return {"success": True, "statuses": statuses}
```

- [ ] **Step 5: 跑测试确认全绿**

```bash
cd E:/online/workspace/copiioai/langsistance && \
  REDIS_HOST=127.0.0.1 REDIS_PORT=6379 PYTHONUTF8=1 \
  python -m pytest tests/test_long_task_api.py -v
```

预期：全部 PASS。

- [ ] **Step 6: 核实 web 调用点未被破坏（**不要跳过**）**

spec §4 要求先确认 nextjs 轮询的都是自己创建的任务。逐个读并确认：

```bash
cd E:/online/workspace/copiioai/langsistance && \
  grep -n "long_task/batch_status\|long_task/.*status\|recoverLongTask" \
    frontend/nextjs/services/api.ts frontend/nextjs/lib/useChatStream.ts \
    frontend/nextjs/lib/longTaskRecovery.ts
```

对每一处确认：`task_id` 来源是本次会话中后端返回的 `long_task_created`，而非用户输入或 URL 参数。
**若发现任何一处会查他人任务，停下来报告，不要继续。**

- [ ] **Step 7: 提交**

```bash
cd E:/online/workspace/copiioai/langsistance && \
  git add api_routes/long_task.py tests/test_long_task_api.py && \
  git commit -m "fix(long_task): status/batch_status 补归属校验——有 token 但可查他人任务"
```

---

### Task 3: 前端地基 —— 抽出 401 处理、导出 SseParser、对齐 push_filter

**Files:**
- Modify: `frontend/weapp/src/services/api.ts`
- Modify: `frontend/weapp/src/services/chatStream.ts`

**Interfaces:**
- Produces:
  - `api.ts`：`clearAuthAndRedirect(message?: string): never`
  - `chatStream.ts`：`export class SseParser`、`export interface SseEvent`、`export interface ArtifactPayload`、`export interface CompletedArtifact`
- Consumes: 无（这是地基任务）

**为什么先做**：后面 Task 6 的 `uploadFile` 走不到 `request()`，拿不到它的 401 处理；`SseParser` 目前是文件内私有类，上传服务解析响应要用。

- [ ] **Step 1: 抽取 401 处理**

`frontend/weapp/src/services/api.ts` —— 在 `request()` 之前插入：

```ts
/**
 * 凭证失效的统一处置：清本地 + 回登录页 + 抛出可见错误。
 * 抽出来是因为 Taro.uploadFile / Taro.downloadFile 走不到 request()，
 * 但同样需要这套处置。
 */
export function clearAuthAndRedirect(message = '登录已失效'): never {
  Taro.removeStorageSync(STORAGE_KEYS.wxToken)
  Taro.removeStorageSync(STORAGE_KEYS.userId)
  Taro.navigateTo({ url: '/pages/login/index' })
  throw new Error(message)
}
```

然后把 `request()` 里的 401 分支：

```ts
  if (resp.statusCode === 401) {
    // 凭证失效/未登录 —— 清本地并回登录页
    Taro.removeStorageSync(STORAGE_KEYS.wxToken)
    Taro.removeStorageSync(STORAGE_KEYS.userId)
    Taro.navigateTo({ url: '/pages/login/index' })
    throw new Error((body && (body.detail || body.message)) || '登录已失效')
  }
```

替换为：

```ts
  if (resp.statusCode === 401) {
    clearAuthAndRedirect((body && (body.detail || body.message)) || '登录已失效')
  }
```

- [ ] **Step 2: 导出 SseParser 与 SseEvent**

`frontend/weapp/src/services/chatStream.ts` —— 把 `interface SseEvent`（`:23`）改为 `export interface SseEvent`，把 `class SseParser`（`:57`）改为 `export class SseParser`。

- [ ] **Step 3: 补 artifact 类型**

`frontend/weapp/src/services/chatStream.ts` —— 在 `StreamCallbacks` 之前插入：

```ts
/** artifact_start 帧的元信息（后端把 metadata 平铺进 start 帧）。 */
export interface ArtifactPayload {
  format: string
  filename: string
  mimeType: string
  rowCount: number
  columnCount: number
}

/** artifact_end 后交付给页面的一份完整工件。 */
export interface CompletedArtifact extends ArtifactPayload {
  artifactId: string
  chunks: string[]
}
```

并在 `StreamCallbacks` 中追加一行：

```ts
  /** 一份工件收齐后回调一次（不是每个 chunk 都回调）。 */
  onArtifactsReady?: (items: CompletedArtifact[]) => void
```

- [ ] **Step 4: 对齐 push_filter**

`frontend/weapp/src/services/chatStream.ts:148` 的 `push_filter: null,` 改为 `push_filter: 2,`。

原因：web 两条路径都发 `2`（`frontend/nextjs/services/api.ts:117` 数字、`:141` 字符串），后端 `sources/knowledge/query_filters.py:74-76` 收到 `None` 直接返回空条件——**等于小程序此前完全没走知识库推送过滤**。

- [ ] **Step 5: 类型检查**

```bash
cd E:/online/workspace/copiioai/langsistance/frontend/weapp && npm run tsc
```

预期：exit 0，无输出。

- [ ] **Step 6: 提交**

```bash
cd E:/online/workspace/copiioai/langsistance && \
  git add frontend/weapp/src/services/api.ts frontend/weapp/src/services/chatStream.ts && \
  git commit -m "refactor(weapp): 抽出 401 处置与 SseParser/工件类型，push_filter 对齐 web"
```

---

### Task 4: 前端 —— 隐私授权（上传的硬前置）

**Files:**
- Create: `frontend/weapp/src/services/privacy.ts`
- Create: `frontend/weapp/src/components/PrivacyPopup/index.tsx`
- Create: `frontend/weapp/src/components/PrivacyPopup/index.scss`
- Modify: `frontend/weapp/src/app.ts`
- Modify: `frontend/weapp/src/pages/chat/index.tsx`（挂载浮层）
- Modify: `frontend/weapp/src/pages/chat/index.scss`

**Interfaces:**
- Produces:
  - `privacy.ts`：`registerPrivacyHandler(): void`、`subscribePrivacy(cb: (visible: boolean) => void): () => void`、`resolvePrivacy(agree: boolean): void`
  - `PrivacyPopup`：`{ visible: boolean; onAgree: () => void; onDecline: () => void }`

**背景**：`wx.chooseMessageFile` 在微信隐私接口清单内。**未在公众平台声明「收集你选中的文件」，该接口直接被禁用**——不是调用失败，是压根调不起来。所以这是上传功能的硬前置，必须先做。

授权顺序必须是**先授权、再选文件**，否则用户点回形针 → 选完文件 → 才弹协议，白选一次。

- [ ] **Step 1: 写 privacy service**

创建 `frontend/weapp/src/services/privacy.ts`：

```ts
import Taro from '@tarojs/taro'

/**
 * 隐私授权闸口（微信 2023-09 新规）。
 *
 * wx.chooseMessageFile 属隐私接口，未在公众平台《小程序用户隐私保护指引》
 * 声明「收集你选中的文件」时**接口直接禁用**（不是调用失败，是压根不存在）。
 * 故这里在真正调用前先确保用户已同意。
 *
 * 流程：调用方 await ensurePrivacy() → 若已同意立即返回；否则触发
 * onNeedPrivacyAuthorization，由页面把浮层显示出来，用户点同意后
 * resolve({ event: 'agree' }) 使本 Promise 兑现。
 */
type Resolver = (arg: { buttonId?: string; event: 'agree' | 'disagree' }) => void

let pendingResolve: Resolver | null = null
let listener: ((visible: boolean) => void) | null = null
let registered = false

/** App 级注册一次（app.ts 的 useLaunch 调用）。 */
export function registerPrivacyHandler(): void {
  if (registered) return
  registered = true
  if (typeof Taro.onNeedPrivacyAuthorization !== 'function') {
    // 基础库过低（<2.32.3）或非微信端：不做拦截，交给微信默认行为
    return
  }
  Taro.onNeedPrivacyAuthorization((resolve: any) => {
    pendingResolve = resolve
    listener?.(true)
  })
}

/** 页面订阅浮层显示状态；返回取消订阅函数。 */
export function subscribePrivacy(cb: (visible: boolean) => void): () => void {
  listener = cb
  return () => {
    if (listener === cb) listener = null
  }
}

/** 用户在浮层上的选择。agree 取自 <button open-type="agreePrivacyAuthorization">。 */
export function resolvePrivacy(agree: boolean, buttonId = ''): void {
  const resolve = pendingResolve
  pendingResolve = null
  listener?.(false)
  resolve?.({ buttonId, event: agree ? 'agree' : 'disagree' })
}

/**
 * 在调用任何隐私接口之前 await 这个。
 * 已同意过 / 基础库不支持 → 立即兑现；否则等浮层结果。
 */
export function ensurePrivacy(): Promise<void> {
  return new Promise((resolve) => {
    if (typeof Taro.requirePrivacyAuthorize !== 'function') {
      resolve()
      return
    }
    Taro.requirePrivacyAuthorize({
      success: () => resolve(),
      fail: () => {
        // 用户拒绝：浮层已经收起来了，这里只负责让调用方继续（
        // 后续真正的 chooseMessageFile 会因未授权而失败，由调用方提示）
        resolve()
      },
    })
  })
}
```

- [ ] **Step 2: 写浮层组件**

创建 `frontend/weapp/src/components/PrivacyPopup/index.tsx`：

```tsx
import { Button, Text, View } from '@tarojs/components'
import './index.scss'

type Props = {
  visible: boolean
  onAgree: () => void
  onDecline: () => void
}

/**
 * 隐私授权浮层。必须用 <button open-type="agreePrivacyAuthorization">
 * 收集用户同意——普通 onClick 不算数，微信不认。
 */
export default function PrivacyPopup({ visible, onAgree, onDecline }: Props) {
  if (!visible) return null
  return (
    <View className='privacy-mask'>
      <View className='privacy-panel'>
        <Text className='privacy-title'>隐私保护指引</Text>
        <Text className='privacy-body'>
          为完成专利文档分析，我们需要读取你在微信会话中选择的文件。
          文件仅用于本次分析，不会用于其他用途。
          请阅读并同意《隐私保护指引》后继续。
        </Text>
        <View className='privacy-actions'>
          <Button className='privacy-btn privacy-btn-ghost' onClick={onDecline}>
            暂不同意
          </Button>
          <Button
            className='privacy-btn privacy-btn-primary'
            openType='agreePrivacyAuthorization'
            onAgreePrivacyAuthorization={onAgree}
          >
            同意并继续
          </Button>
        </View>
      </View>
    </View>
  )
}
```

创建 `frontend/weapp/src/components/PrivacyPopup/index.scss`：

```scss
.privacy-mask {
  position: fixed;
  inset: 0;
  z-index: 1000;
  background: rgba(0, 0, 0, 0.45);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0 48px;
  box-sizing: border-box;
}

.privacy-panel {
  width: 100%;
  background: var(--c-surface);
  border-radius: 24px;
  padding: 44px 40px 32px;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
}

.privacy-title {
  font-size: var(--fs-title);
  font-weight: 600;
  text-align: center;
}

.privacy-body {
  margin-top: 24px;
  font-size: var(--fs-meta);
  line-height: 1.7;
  color: var(--c-text-muted);
}

.privacy-actions {
  display: flex;
  margin-top: 36px;
  gap: 20px;
}

.privacy-btn {
  flex: 1;
  height: 80px;
  line-height: 80px;
  border-radius: 999px;
  font-size: var(--fs-body);

  &::after {
    border: none;
  }
}

.privacy-btn-ghost {
  background: var(--c-bg);
  color: var(--c-text-muted);
}

.privacy-btn-primary {
  background: var(--c-primary);
  color: #fff;

  &:active {
    background: var(--c-primary-deep);
  }
}
```

- [ ] **Step 3: 注册到 App**

`frontend/weapp/src/app.ts` 整体替换为：

```tsx
import { PropsWithChildren } from 'react'
import { useLaunch } from '@tarojs/taro'
import { registerPrivacyHandler } from './services/privacy'
import './app.scss'

function App({ children }: PropsWithChildren<any>) {
  useLaunch(() => {
    // 隐私授权监听必须在 App 级注册一次——页面级注册会在切页时丢失
    // pendingResolve，导致用户点同意后 Promise 永远不兑现。
    registerPrivacyHandler()
  })
  return children
}

export default App
```

- [ ] **Step 4: 接入 chat 页**

`frontend/weapp/src/pages/chat/index.tsx`：

顶部 import 区加：

```tsx
import PrivacyPopup from '../../components/PrivacyPopup'
import { resolvePrivacy, subscribePrivacy } from '../../services/privacy'
```

组件内 state 区（`const [anchor, setAnchor] = useState('')` 附近）加：

```tsx
  // 隐私授权浮层
  const [privacyVisible, setPrivacyVisible] = useState(false)
```

在 `useEffect(() => () => stopTimer(), [])` 之后加：

```tsx
  // 订阅隐私浮层显隐（App 级监听在 app.ts 注册）
  useEffect(() => subscribePrivacy(setPrivacyVisible), [])
```

在 `</View>` 收尾前（`<RenameModal ... />` 之后）加：

```tsx
      <PrivacyPopup
        visible={privacyVisible}
        onAgree={() => resolvePrivacy(true)}
        onDecline={() => resolvePrivacy(false)}
      />
```

**页面 scss 不要加 `@import`。** 组件的 `PrivacyPopup/index.tsx` 里已有 `import './index.scss'`，Taro 会把它内联进 `dist/pages/chat/index.wxss`——这是本仓库既有模式（三个既有组件都如此，页面 scss 里一处 `@import` 都没有）。再在页面里 `@import` 一次会产出重复样式。

- [ ] **Step 5: 类型检查 + 构建**

```bash
cd E:/online/workspace/copiioai/langsistance/frontend/weapp && npm run tsc && npm run build:weapp
```

预期：`tsc` exit 0；Webpack `Compiled successfully`。

- [ ] **Step 6: 人工验证（开发者工具）**

1. **完全重启**开发者工具（`compileHotReLoad` 已关，不重启看不到新代码）
2. 导入路径确认是 `frontend/weapp/dist`，不要切换
3. 打开页面，确认无报错

注意：**这一步只能验证浮层不崩、不显示**。真实弹出需要公众平台先声明隐私指引（见 Task 5 Step 6），在那之前 `onNeedPrivacyAuthorization` 不会触发。

- [ ] **Step 7: 提交**

```bash
cd E:/online/workspace/copiioai/langsistance && \
  git add frontend/weapp/src/services/privacy.ts \
          frontend/weapp/src/components/PrivacyPopup \
          frontend/weapp/src/app.ts \
          frontend/weapp/src/pages/chat/index.tsx \
          frontend/weapp/src/pages/chat/index.scss && \
  git commit -m "feat(weapp): 隐私授权浮层——chooseMessageFile 是隐私接口，未授权直接禁用"
```

---

### Task 5: 前端 —— 隐私政策页与登录页入口

**Files:**
- Create: `frontend/weapp/src/pages/privacy/index.tsx`
- Create: `frontend/weapp/src/pages/privacy/index.config.ts`
- Create: `frontend/weapp/src/pages/privacy/index.scss`
- Modify: `frontend/weapp/src/app.config.ts`
- Modify: `frontend/weapp/src/pages/login/index.tsx`
- Modify: `frontend/weapp/src/pages/login/index.scss`

**Interfaces:**
- Consumes: 无
- Produces: 路由 `/pages/privacy/index`

**背景**：现有 web 隐私政策（`frontend/nextjs/app/(landing)/privacy-policy/page.tsx`）是英文、写的是 Chrome 扩展权限、更新于 2026-03-05，语境不符，**不能直接复用**。需针对小程序重写中文版。

**本任务的文本内容只覆盖已知事实**：登录用 `wx.login` 换 openid（不取昵称头像手机号）、选取的文件用于分析。**不要编造**未实现的能力（如定位、通讯录）——不实声明在提审时是驳回项。

- [ ] **Step 1: 写隐私政策页**

创建 `frontend/weapp/src/pages/privacy/index.config.ts`：

```ts
export default {
  navigationBarTitleText: '隐私政策',
}
```

创建 `frontend/weapp/src/pages/privacy/index.tsx`：

```tsx
import { ScrollView, Text, View } from '@tarojs/components'
import './index.scss'

/**
 * 小程序隐私政策。
 * 与 web 的 /privacy-policy 是两份独立文本——那份写的是 Chrome 扩展权限，
 * 语境不同，不可混用。
 */
export default function PrivacyPage() {
  return (
    <ScrollView className='privacy-page' scrollY>
      <View className='privacy-doc'>
        <Text className='privacy-doc-title'>CopiioAI 专利助手隐私政策</Text>
        <Text className='privacy-doc-date'>更新日期：2026-09-11</Text>

        <Text className='privacy-h'>1. 我们收集哪些信息</Text>
        <Text className='privacy-p'>
          1.1 微信登录标识。你使用微信一键登录时，我们会通过微信提供的登录凭证
          （code）换取你的微信用户唯一标识（openid），用于识别你的账号并关联你的
          历史对话。我们不会获取你的微信昵称、头像或手机号。
        </Text>
        <Text className='privacy-p'>
          1.2 你主动提交的内容。包括你输入的提问文字，以及你从微信会话中选择上传的
          专利文档（PDF / DOCX / XML）。这些内容用于执行你请求的检索与分析。
        </Text>
        <Text className='privacy-p'>
          1.3 对话记录。你的提问与相应的分析结果会保存为历史会话，以便你随时回看。
        </Text>

        <Text className='privacy-h'>2. 我们如何使用这些信息</Text>
        <Text className='privacy-p'>
          仅用于提供专利检索、专利分析与相关结果导出服务，以及维护你的历史会话。
        </Text>

        <Text className='privacy-h'>3. 信息的存储与保留</Text>
        <Text className='privacy-p'>
          上述信息存储在我们的服务器上。你可以在小程序内删除任意历史会话，删除后该
          会话不再展示。
        </Text>

        <Text className='privacy-h'>4. 信息的共享</Text>
        <Text className='privacy-p'>
          我们不会将你的个人信息出售或提供给第三方用于营销目的。为完成你请求的分析，
          提问内容会发送至第三方大语言模型服务处理。
        </Text>

        <Text className='privacy-h'>5. 你的权利</Text>
        <Text className='privacy-p'>
          你可以随时在小程序内删除历史会话；如需注销账号或删除全部数据，可通过下方
          联系方式与我们联系。
        </Text>

        <Text className='privacy-h'>6. 联系我们</Text>
        <Text className='privacy-p'>copiioai.com@gmail.com</Text>
      </View>
    </ScrollView>
  )
}
```

创建 `frontend/weapp/src/pages/privacy/index.scss`：

```scss
.privacy-page {
  height: 100vh;
  background: var(--c-bg);
}

.privacy-doc {
  padding: 32px 36px 80px;
  display: flex;
  flex-direction: column;
}

.privacy-doc-title {
  font-size: var(--fs-title);
  font-weight: 600;
  line-height: 1.4;
}

.privacy-doc-date {
  margin-top: 12px;
  font-size: var(--fs-meta);
  color: var(--c-text-muted);
}

.privacy-h {
  margin-top: 44px;
  font-size: var(--fs-msg);
  font-weight: 600;
  line-height: 1.5;
}

.privacy-p {
  margin-top: 16px;
  font-size: var(--fs-body);
  line-height: 1.75;
  color: var(--c-text);
}
```

- [ ] **Step 2: 注册路由**

`frontend/weapp/src/app.config.ts` —— `pages` 改为：

```ts
  pages: [
    'pages/chat/index',    // 对话页（形态一：首页即对话页）
    'pages/login/index',   // 微信登录（M1）
    'pages/privacy/index', // 隐私政策（M3）
  ],
```

- [ ] **Step 3: 登录页加入口**

`frontend/weapp/src/pages/login/index.tsx` —— 把末尾的：

```tsx
      <Text className='login-legal text-muted'>
        登录即代表同意《用户协议》与《隐私政策》
      </Text>
```

替换为：

```tsx
      <Text className='login-legal text-muted'>
        登录即代表同意
        <Text
          className='login-legal-link'
          onClick={() => Taro.navigateTo({ url: '/pages/privacy/index' })}
        >
          《隐私政策》
        </Text>
      </Text>
```

（原文提到《用户协议》但仓库里并不存在该页面——见 spec §6，不虚构入口，只保留实际存在的《隐私政策》。）

`frontend/weapp/src/pages/login/index.scss` 末尾追加：

```scss
.login-legal-link {
  color: var(--c-primary-deep);
}
```

- [ ] **Step 4: 类型检查 + 构建**

```bash
cd E:/online/workspace/copiioai/langsistance/frontend/weapp && npm run tsc && npm run build:weapp
```

预期：`tsc` exit 0；`Compiled successfully`。

- [ ] **Step 5: 人工验证**

开发者工具完全重启后：登录页点《隐私政策》→ 进入隐私页并正确渲染；返回可回到登录页。

- [ ] **Step 6: 平台配置（非代码，必须做，否则 Task 6 的上传跑不起来）**

在微信公众平台（mp.weixin.qq.com）→ 设置 → 服务内容声明 → 用户隐私保护指引：
- 声明信息类型：**「收集你选中的文件」**
- 对应的接口 `wx.chooseMessageFile`

**未声明时接口直接禁用**，代码里的授权浮层也不会生效。把这一步写进 `deploy/china/RUNBOOK_GO_LIVE.md` 的上线清单。

- [ ] **Step 7: 提交**

```bash
cd E:/online/workspace/copiioai/langsistance && \
  git add frontend/weapp/src/pages/privacy frontend/weapp/src/app.config.ts \
          frontend/weapp/src/pages/login && \
  git commit -m "feat(weapp): 小程序隐私政策页 + 登录页入口"
```

---

### Task 6: 前端 —— 上传服务

**Files:**
- Create: `frontend/weapp/src/services/upload.ts`

**Interfaces:**
- Consumes: `clearAuthAndRedirect`（Task 3）、`SseParser`（Task 3）、`ensurePrivacy`（Task 4）
- Produces:
  - `PickedFile { path: string; name: string; size: number }`
  - `pickFile(): Promise<PickedFile | null>` —— 已含隐私授权与校验，取消/校验失败返回 `null`
  - `uploadQuery(file, query, history, queryId, sessionId?, onProgress?): Promise<UploadOutcome>`
  - `UploadOutcome { taskId: string; sessionId: string; patentCount: number }`

**关键约束**：`wx.uploadFile` 只能单文件、没有 `onChunkReceived`。上传分支的 SSE 只有 `long_task_created` + `end` 两帧，读完整 `res.data` 再解析即可。

- [ ] **Step 1: 写 upload service**

创建 `frontend/weapp/src/services/upload.ts`：

```ts
import Taro from '@tarojs/taro'
import { API_BASE, STORAGE_KEYS } from '../config'
import { clearAuthAndRedirect } from './api'
import { ChatMsg } from './chat'
import { SseParser } from './chatStream'
import { ensurePrivacy } from './privacy'

/** 与 web 及后端同值：api_routes/core.py:953-954、nextjs useChatStream.ts:64 */
export const MAX_FILE_BYTES = 10 * 1024 * 1024
/** 后端丢弃 <50 字节的文件（api_routes/core.py:967），前端提前拦 */
export const MIN_FILE_BYTES = 50
export const ALLOWED_EXTENSIONS = ['.pdf', '.docx', '.xml']

export interface PickedFile {
  path: string
  name: string
  size: number
}

export interface UploadOutcome {
  taskId: string
  sessionId: string
  patentCount: number
}

export function extensionOf(name: string): string {
  const i = name.lastIndexOf('.')
  return i < 0 ? '' : name.slice(i).toLowerCase()
}

/** 通过返回 null；不通过返回给用户看的原因。 */
export function validateFile(file: { name: string; size: number }): string | null {
  if (!ALLOWED_EXTENSIONS.includes(extensionOf(file.name))) {
    return '仅支持 PDF / DOCX / XML 格式'
  }
  if (file.size > MAX_FILE_BYTES) return '文件超过 10MB 上限'
  if (file.size < MIN_FILE_BYTES) return '文件内容为空或过小'
  return null
}

/**
 * 选一个文件。内部先过隐私授权——wx.chooseMessageFile 是隐私接口，
 * 未授权会被微信直接禁用，先选后授权会让用户白选一次。
 * 用户取消或校验不过返回 null（已 toast 提示）。
 */
export async function pickFile(): Promise<PickedFile | null> {
  await ensurePrivacy()
  let res: Taro.chooseMessageFile.SuccessCallbackResult
  try {
    res = await Taro.chooseMessageFile({
      count: 1,
      type: 'file',
      extension: ['pdf', 'docx', 'xml'],
    })
  } catch {
    return null // 用户取消
  }
  const f = res.tempFiles && res.tempFiles[0]
  if (!f) return null
  const picked: PickedFile = { path: f.path, name: f.name, size: f.size }
  const reason = validateFile(picked)
  if (reason) {
    Taro.showToast({ title: reason, icon: 'none' })
    return null
  }
  return picked
}

/**
 * 上传单个文件并发起长任务。字段名与 web 完全一致
 * （frontend/nextjs/services/api.ts:139-146）。
 *
 * 注意 conversation_history 必须**包含本轮新提问**——后端会把它直接写成
 * 会话的 messages（api_routes/core.py:1041），不含新提问则该轮不落库。
 */
export async function uploadQuery(
  file: PickedFile,
  query: string,
  conversationHistory: ChatMsg[],
  queryId: string,
  sessionId?: string,
  onProgress?: (percent: number) => void,
): Promise<UploadOutcome> {
  const token = Taro.getStorageSync(STORAGE_KEYS.wxToken)
  const formData: Record<string, string> = {
    query,
    query_id: queryId,
    // multipart 的 formData 值只能是字符串，故此处是 '2' 而非数字 2，
    // 与 web 的 formData 分支一致（nextjs services/api.ts:141）
    push_filter: '2',
    conversation_history: JSON.stringify(conversationHistory),
  }
  if (sessionId) formData.session_id = sessionId

  const task = Taro.uploadFile({
    url: `${API_BASE}/query_stream`,
    filePath: file.path,
    name: 'patent_files',
    formData,
    header: { Authorization: `Bearer ${token}` },
    timeout: 120000,
  })
  if (onProgress && typeof task.onProgressUpdate === 'function') {
    task.onProgressUpdate((r) => onProgress(r.progress))
  }

  const res = await task
  const raw = String(res.data || '')

  if (res.statusCode === 401) clearAuthAndRedirect()
  if (res.statusCode >= 400) {
    // 后端错误体是 JSON（FastAPI HTTPException），尽力取可读文案
    let detail = ''
    try {
      const parsed = JSON.parse(raw)
      detail = parsed.detail || parsed.message || ''
    } catch {
      detail = ''
    }
    throw new Error(detail || `上传失败(${res.statusCode})`)
  }

  for (const ev of new SseParser().push(raw)) {
    if (ev.type === 'long_task_created') {
      return {
        taskId: String(ev.task_id || ''),
        sessionId: String(ev.session_id || ''),
        patentCount: Number(ev.patent_count || 0),
      }
    }
    if (ev.type === 'error') {
      throw new Error(String(ev.message || ev.content || '文件处理失败'))
    }
  }
  throw new Error('上传已完成但未返回任务号，请稍后在历史会话中查看')
}
```

- [ ] **Step 2: 类型检查**

```bash
cd E:/online/workspace/copiioai/langsistance/frontend/weapp && npm run tsc
```

预期：exit 0。若报 `Taro.chooseMessageFile.SuccessCallbackResult` 不存在，改用：

```ts
  let res: { tempFiles: Array<{ path: string; name: string; size: number }> }
```

- [ ] **Step 3: 构建**

```bash
cd E:/online/workspace/copiioai/langsistance/frontend/weapp && npm run build:weapp
```

预期：`Compiled successfully`。

- [ ] **Step 4: 提交**

```bash
cd E:/online/workspace/copiioai/langsistance && \
  git add frontend/weapp/src/services/upload.ts && \
  git commit -m "feat(weapp): 上传服务——chooseMessageFile + uploadFile + 解析 long_task_created"
```

---

### Task 7: 前端 —— 附件 chip 条与输入栏接入

**Files:**
- Create: `frontend/weapp/src/components/AttachmentBar/index.tsx`
- Create: `frontend/weapp/src/components/AttachmentBar/index.scss`
- Modify: `frontend/weapp/src/pages/chat/index.tsx`
- Modify: `frontend/weapp/src/pages/chat/index.scss`

**Interfaces:**
- Consumes: `PickedFile`（Task 6）
- Produces: `AttachmentBar` 的 props `{ file: PickedFile | null; onAdd: () => void; onRemove: () => void; busy?: boolean }`

**对齐 web**：`frontend/nextjs/components/app/ChatComposer.tsx:95-117` 的 chip 条（类型徽标 + 文件名 + 大小 + ✕）与 `:131-140` 的回形针按钮。

- [ ] **Step 1: 写组件**

创建 `frontend/weapp/src/components/AttachmentBar/index.tsx`：

```tsx
import { Text, View } from '@tarojs/components'
import { PickedFile, extensionOf } from '../../services/upload'
import './index.scss'

type Props = {
  file: PickedFile | null
  busy?: boolean
  onAdd: () => void
  onRemove: () => void
}

/** 对齐 web 的 getFileTypeBadge（ChatComposer.tsx:80-85）。 */
export function fileBadge(name: string): string {
  const ext = extensionOf(name)
  if (ext === '.docx') return 'DOCX'
  if (ext === '.xml') return 'XML'
  return 'PDF'
}

/** 对齐 web 的 formatFileSize（ChatComposer.tsx:87-91）。 */
export function formatFileSize(bytes: number): string {
  if (bytes < 1024) return `${bytes} B`
  if (bytes < 1024 * 1024) return `${(bytes / 1024).toFixed(1)} KB`
  return `${(bytes / (1024 * 1024)).toFixed(1)} MB`
}

export default function AttachmentBar({ file, busy, onAdd, onRemove }: Props) {
  return (
    <View className='attach-bar'>
      <View
        className={`attach-btn${busy ? ' attach-btn-busy' : ''}`}
        onClick={busy ? undefined : onAdd}
      >
        <Text className='attach-btn-icon'>＋</Text>
      </View>
      {file ? (
        <View className='attach-chip'>
          <Text className='attach-chip-badge'>{fileBadge(file.name)}</Text>
          <Text className='attach-chip-name'>{file.name}</Text>
          <Text className='attach-chip-size'>{formatFileSize(file.size)}</Text>
          <Text className='attach-chip-remove' onClick={onRemove}>
            ✕
          </Text>
        </View>
      ) : null}
    </View>
  )
}
```

创建 `frontend/weapp/src/components/AttachmentBar/index.scss`：

```scss
.attach-bar {
  display: flex;
  align-items: center;
  padding: 12px 20px 0;
  background: var(--c-surface);
}

.attach-btn {
  flex-shrink: 0;
  width: 64px;
  height: 64px;
  border-radius: 50%;
  background: var(--c-bg);
  border: 1px solid var(--c-border);
  display: flex;
  align-items: center;
  justify-content: center;

  &:active {
    background: var(--c-primary-soft);
  }
}

.attach-btn-busy {
  opacity: 0.45;
}

.attach-btn-icon {
  font-size: 36px;
  line-height: 1;
  color: var(--c-text-muted);
}

.attach-chip {
  flex: 1;
  min-width: 0;
  display: flex;
  align-items: center;
  margin-left: 16px;
  padding: 12px 18px;
  background: var(--c-primary-soft);
  border-radius: 14px;
}

.attach-chip-badge {
  flex-shrink: 0;
  padding: 2px 10px;
  border-radius: 6px;
  background: var(--c-primary);
  color: #fff;
  font-size: 20px;
  font-weight: 600;
}

.attach-chip-name {
  flex: 1;
  min-width: 0;
  margin-left: 12px;
  font-size: var(--fs-meta);
  color: var(--c-text);
  overflow: hidden;
  white-space: nowrap;
  text-overflow: ellipsis;
}

.attach-chip-size {
  flex-shrink: 0;
  margin-left: 12px;
  font-size: 20px;
  color: var(--c-text-muted);
}

.attach-chip-remove {
  flex-shrink: 0;
  margin-left: 14px;
  padding: 0 6px;
  font-size: 24px;
  color: var(--c-text-muted);
}
```

- [ ] **Step 2: 接进 chat 页**

`frontend/weapp/src/pages/chat/index.tsx`：

import 区加：

```tsx
import AttachmentBar from '../../components/AttachmentBar'
import { pickFile, PickedFile, uploadQuery } from '../../services/upload'
```

state 区加：

```tsx
  const [attachedFile, setAttachedFile] = useState<PickedFile | null>(null)
```

在 `copyPatent` 之前加：

```tsx
  async function addAttachment() {
    const picked = await pickFile()
    if (picked) setAttachedFile(picked)
  }
```

把输入栏 JSX：

```tsx
      <View className='chat-inputbar'>
        <Textarea
```

改为：

```tsx
      <AttachmentBar
        file={attachedFile}
        busy={sending}
        onAdd={addAttachment}
        onRemove={() => setAttachedFile(null)}
      />

      <View className='chat-inputbar'>
        <Textarea
```

**发送按钮的禁用条件保持 `disabled={sending || !input.trim()}` 不动。**

显式写出来是因为它容易被"顺手"改错：web 的 `ChatComposer.tsx:165` 在"有文件无文字"时按钮**可点**，但 `useChatStream.ts:119` 的 `if (!text || streaming) return` 让点击**静默无效**——那是 web 的 bug。小程序保持禁用，理由见 spec §3.2。

**页面 scss 不要加 `@import`**——`AttachmentBar/index.tsx` 里的 `import './index.scss'` 已足够（同 Task 4 的说明）。

- [ ] **Step 3: 类型检查 + 构建**

```bash
cd E:/online/workspace/copiioai/langsistance/frontend/weapp && npm run tsc && npm run build:weapp
```

预期：exit 0 + `Compiled successfully`。

- [ ] **Step 4: 人工验证**

开发者工具完全重启后：点 `＋` → 选一个 PDF → 出现 chip 显示文件名与大小 → 点 ✕ 可移除。

**若点 `＋` 无反应**：说明公众平台隐私指引尚未声明（Task 5 Step 6），或当前在开发者工具中未开通隐私调试。先完成平台配置再验证。

- [ ] **Step 5: 提交**

```bash
cd E:/online/workspace/copiioai/langsistance && \
  git add frontend/weapp/src/components/AttachmentBar \
          frontend/weapp/src/pages/chat/index.tsx \
          frontend/weapp/src/pages/chat/index.scss && \
  git commit -m "feat(weapp): 附件 chip 条与输入栏接入"
```

---

### Task 8: 前端 —— 长任务轮询服务

**Files:**
- Create: `frontend/weapp/src/services/longTask.ts`

**Interfaces:**
- Consumes: `request`（`services/api.ts`）
- Produces:
  - `TaskStatus`、`TaskReportFile`、`TaskState`
  - `TaskState { taskId; status; phase; progress; step; reportFiles; error }`
  - `isTerminal(status: TaskStatus): boolean`
  - `pollTasks(taskIds: string[]): Promise<Record<string, TaskState>>`
  - `retryTask(taskId: string): Promise<string>`
  - `PHASE_LABELS: Record<string, string>`
  - `POLL_INTERVAL_MS = 1500`、`MAX_POLL_FAILURES = 5`

- [ ] **Step 1: 写服务**

创建 `frontend/weapp/src/services/longTask.ts`：

```ts
import { request } from './api'

/** 与 sources/long_task/status_manager.py 的状态值一致。 */
export type TaskStatus =
  | 'pending'
  | 'queued'
  | 'running'
  | 'paused'
  | 'cancelling'
  | 'completed'
  | 'failed'
  | 'cancelled'
  | 'unknown'

export interface TaskReportFile {
  format: string
  filename: string
  size: number
}

export interface TaskState {
  taskId: string
  status: TaskStatus
  phase: string
  progress: number
  step: string
  reportFiles: TaskReportFile[]
  error: string
}

/** web 同节奏（frontend/nextjs/lib/useChatStream.ts:629-630） */
export const POLL_INTERVAL_MS = 1500
/** 连续失败这么多次后判定"状态未知"并停止轮询 */
export const MAX_POLL_FAILURES = 5

/** 阶段文案对齐 web 的 STANDARD_PHASES（LongTaskProgress.tsx:224-231） */
export const PHASE_LABELS: Record<string, string> = {
  extracting_text: '提取文本',
  searching_patents: '检索专利',
  generating_columns: '生成分析维度',
  analyzing: '逐件分析',
  generating_report: '生成报告',
  exporting: '导出文件',
}

const TERMINAL: TaskStatus[] = ['completed', 'failed', 'cancelled', 'unknown']

export function isTerminal(status: TaskStatus): boolean {
  return TERMINAL.indexOf(status) >= 0
}

export function phaseLabel(phase: string): string {
  return PHASE_LABELS[phase] || phase || '处理中'
}

/** 把后端 status 载荷归一成 TaskState。 */
export function toTaskState(taskId: string, raw: Record<string, any>): TaskState {
  const files = Array.isArray(raw.report_files) ? raw.report_files : []
  return {
    taskId,
    status: (raw.status || 'unknown') as TaskStatus,
    phase: String(raw.current_phase || ''),
    progress: Number(raw.progress || 0),
    step: String(raw.current_step || ''),
    reportFiles: files.map((f: any) => ({
      format: String(f.format || ''),
      filename: String(f.filename || ''),
      size: Number(f.size || 0),
    })),
    error: String(raw.error_message || ''),
  }
}

/** 批量取状态。后端只返回属于本人的任务（api_routes/long_task.py:561+），
 *  故返回的键可能少于传入的 taskIds。 */
export async function pollTasks(
  taskIds: string[],
): Promise<Record<string, TaskState>> {
  if (taskIds.length === 0) return {}
  const body = await request<{ statuses: Record<string, any> }>(
    '/long_task/batch_status',
    { method: 'POST', data: { task_ids: taskIds.slice(0, 20) } },
  )
  const out: Record<string, TaskState> = {}
  const statuses = (body && body.statuses) || {}
  for (const tid of taskIds.slice(0, 20)) {
    const raw = statuses[tid]
    if (raw) out[tid] = toTaskState(tid, raw)
  }
  return out
}

/** 一键重试，返回新任务的 task_id（api_routes/long_task.py:426-541）。 */
export async function retryTask(taskId: string): Promise<string> {
  const body = await request<{ task_id: string }>(
    `/long_task/${taskId}/retry`,
    { method: 'POST' },
  )
  return body.task_id
}
```

- [ ] **Step 2: 类型检查**

```bash
cd E:/online/workspace/copiioai/langsistance/frontend/weapp && npm run tsc
```

预期：exit 0。

- [ ] **Step 3: 提交**

```bash
cd E:/online/workspace/copiioai/langsistance && \
  git add frontend/weapp/src/services/longTask.ts && \
  git commit -m "feat(weapp): 长任务轮询服务——batch_status 轮询与状态归一"
```

---

### Task 9: 前端 —— 长任务卡片与上传分支接入

**Files:**
- Create: `frontend/weapp/src/components/LongTaskCard/index.tsx`
- Create: `frontend/weapp/src/components/LongTaskCard/index.scss`
- Modify: `frontend/weapp/src/pages/chat/index.tsx`
- Modify: `frontend/weapp/src/pages/chat/index.scss`

**Interfaces:**
- Consumes: `TaskState`、`phaseLabel`、`isTerminal`（Task 8）、`UploadOutcome`（Task 6）
- Produces: `LongTaskCard` props `{ task: TaskState; onRetry: () => void; onDownload: (format: string) => void }`

**两处关键差异（有意为之，见 spec §3.3）**：
1. 状态直接挂在消息对象上（`MsgView.task`），**不做 web 的 `🔬`/`[N%]` 标记编码与正则反解**——小程序的消息本来就是结构化对象。
2. **上传分支完成后不调用 `saveMessages`**：后端在上传请求内就把 `conversation_history` 写成了会话消息（`api_routes/core.py:1041`），随后追加 `created`/`completed`/`failed`；小程序若再 `PUT /messages` 会整体重写数组，**抹掉后端写的内容**。

- [ ] **Step 1: 写卡片组件**

创建 `frontend/weapp/src/components/LongTaskCard/index.tsx`：

```tsx
import { Text, View } from '@tarojs/components'
import { TaskState, isTerminal, phaseLabel } from '../../services/longTask'
import './index.scss'

type Props = {
  task: TaskState
  onRetry: () => void
  onDownload: (format: string) => void
}

export default function LongTaskCard({ task, onRetry, onDownload }: Props) {
  const done = task.status === 'completed'
  const failed = task.status === 'failed' || task.status === 'cancelled'
  const pct = Math.max(0, Math.min(100, task.progress || 0))

  return (
    <View className='ltcard'>
      <View className='ltcard-head'>
        <Text className='ltcard-title'>
          {done ? '分析完成' : failed ? '分析失败' : '正在分析'}
        </Text>
        {!isTerminal(task.status) ? (
          <Text className='ltcard-pct'>{pct}%</Text>
        ) : null}
      </View>

      {!isTerminal(task.status) ? (
        <>
          <View className='ltcard-bar'>
            <View className='ltcard-bar-fill' style={{ width: `${pct}%` }} />
          </View>
          <Text className='ltcard-step'>
            {task.step || phaseLabel(task.phase)}
          </Text>
        </>
      ) : null}

      {failed ? (
        <>
          <Text className='ltcard-error'>{task.error || '任务未能完成'}</Text>
          <View className='ltcard-retry' onClick={onRetry}>
            <Text>重试</Text>
          </View>
        </>
      ) : null}

      {done && task.reportFiles.length > 0 ? (
        <View className='ltcard-files'>
          {task.reportFiles.map((f) => (
            <View
              key={f.format}
              className='ltcard-file'
              onClick={() => onDownload(f.format)}
            >
              <Text className='ltcard-file-badge'>
                {f.format.toUpperCase()}
              </Text>
              <Text className='ltcard-file-label'>下载报告</Text>
            </View>
          ))}
        </View>
      ) : null}
    </View>
  )
}
```

创建 `frontend/weapp/src/components/LongTaskCard/index.scss`：

```scss
.ltcard {
  margin-top: 16px;
  padding: 24px 26px;
  background: var(--c-surface);
  border: 1px solid var(--c-border);
  border-left: 6px solid var(--c-primary);
  border-radius: 14px;
}

.ltcard-head {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
}

.ltcard-title {
  font-size: var(--fs-msg);
  font-weight: 600;
}

.ltcard-pct {
  font-size: var(--fs-meta);
  color: var(--c-primary-deep);
}

.ltcard-bar {
  margin-top: 18px;
  height: 8px;
  border-radius: 4px;
  background: var(--c-bg);
  overflow: hidden;
}

.ltcard-bar-fill {
  height: 100%;
  background: var(--c-primary);
  border-radius: 4px;
}

.ltcard-step {
  display: block;
  margin-top: 14px;
  font-size: var(--fs-meta);
  line-height: 1.6;
  color: var(--c-text-muted);
}

.ltcard-error {
  display: block;
  margin-top: 14px;
  font-size: var(--fs-meta);
  line-height: 1.6;
  color: var(--c-danger);
}

.ltcard-retry {
  margin-top: 18px;
  align-self: flex-start;
  display: inline-block;
  padding: 12px 32px;
  border-radius: 999px;
  background: var(--c-primary);
  color: #fff;
  font-size: var(--fs-meta);
}

.ltcard-files {
  display: flex;
  flex-wrap: wrap;
  margin-top: 20px;
  gap: 16px;
}

.ltcard-file {
  display: flex;
  align-items: center;
  padding: 12px 22px;
  border-radius: 12px;
  background: var(--c-primary-soft);
  border: 1px solid rgba(16, 163, 127, 0.25);

  &:active {
    background: #d8ece6;
  }
}

.ltcard-file-badge {
  font-size: 20px;
  font-weight: 600;
  color: var(--c-primary-deep);
}

.ltcard-file-label {
  margin-left: 12px;
  font-size: var(--fs-meta);
  color: var(--c-primary-deep);
}
```

- [ ] **Step 2: 扩展 MsgView 并接入上传分支**

`frontend/weapp/src/pages/chat/index.tsx`：

`MsgView` 接口追加字段：

```tsx
interface MsgView {
  role: 'user' | 'assistant'
  content: string
  html?: string
  patents?: string[]
  streaming?: boolean
  /** 长任务状态（上传分支专用）。不走 web 的标记编码 + 正则反解——
      小程序的消息本来就是结构化对象。 */
  task?: TaskState
}
```

import 区加：

```tsx
import LongTaskCard from '../../components/LongTaskCard'
import {
  MAX_POLL_FAILURES,
  POLL_INTERVAL_MS,
  isTerminal,
  pollTasks,
  retryTask,
  TaskState,
} from '../../services/longTask'
```

state / ref 区加：

```tsx
  // 长任务轮询：taskId → 连续失败次数
  const pollTimerRef = useRef<ReturnType<typeof setInterval> | null>(null)
  const pollFailRef = useRef<Record<string, number>>({})
```

在 `stopTimer` 之后加：

```tsx
  function stopPolling() {
    if (pollTimerRef.current) {
      clearInterval(pollTimerRef.current)
      pollTimerRef.current = null
    }
  }

  /** 收集当前仍未结束的任务号。 */
  function activeTaskIds(): string[] {
    return msgs
      .filter((m) => m.task && !isTerminal(m.task.status))
      .map((m) => m.task!.taskId)
  }

  function applyTaskStates(states: Record<string, TaskState>) {
    setMsgs((prev) =>
      prev.map((m) =>
        m.task && states[m.task.taskId]
          ? { ...m, task: states[m.task.taskId] }
          : m,
      ),
    )
  }

  function startPolling() {
    if (pollTimerRef.current) return
    pollTimerRef.current = setInterval(async () => {
      const ids = activeTaskIds()
      if (ids.length === 0) {
        stopPolling()
        return
      }
      try {
        const states = await pollTasks(ids)
        pollFailRef.current = {}
        applyTaskStates(states)
        if (ids.every((id) => states[id] && isTerminal(states[id].status))) {
          stopPolling()
        }
      } catch {
        // 连续失败才判死，避免一次抖动就把卡片钉在"状态获取失败"
        const ids2 = activeTaskIds()
        for (const id of ids2) {
          pollFailRef.current[id] = (pollFailRef.current[id] || 0) + 1
        }
        const dead = ids2.filter(
          (id) => pollFailRef.current[id] >= MAX_POLL_FAILURES,
        )
        if (dead.length) {
          setMsgs((prev) =>
            prev.map((m) =>
              m.task && dead.indexOf(m.task.taskId) >= 0
                ? { ...m, task: { ...m.task, status: 'unknown' as const } }
                : m,
            ),
          )
        }
        if (ids2.every((id) => (pollFailRef.current[id] || 0) >= MAX_POLL_FAILURES)) {
          stopPolling()
        }
      }
    }, POLL_INTERVAL_MS)
  }

  // 离开页面停止轮询；回来对未完成任务恢复
  useDidHide(() => stopPolling())
  useDidShow(() => {
    if (activeTaskIds().length > 0) startPolling()
  })

  useEffect(() => () => stopPolling(), [])
```

`useDidShow` 已从 `@tarojs/taro` 导入，需补 `useDidHide`：

```tsx
import Taro, { useDidHide, useDidShow } from '@tarojs/taro'
```

在 `send()` 里，把 `startAssistant()` 之后到 `// 终稿落库` 那段改成分支。把：

```tsx
      startAssistant()
      let completed = false
      try {
        await streamQuery(text, history, {
```

到

```tsx
      // 终稿落库（web 同款持久化）
      const assistantMsg: ChatMsg = {
        role: 'assistant',
        content: assistantRef.current,
      }
      await saveMessages(sid, [...history, userMsg, assistantMsg])
```

整体替换为：

```tsx
      startAssistant()

      if (file) {
        // ── 上传分支：单文件 → 长任务 ──
        // conversation_history 必须包含本轮新提问：后端会把它直接写成会话的
        // messages（api_routes/core.py:1041），不含新提问则该轮不落库。
        const outcome = await uploadQuery(
          file,
          text,
          [...history, userMsg],
          newQueryId(),
          sid,
        )
        // 后端可能复用/新建了别的 session_id，以它为准
        if (outcome.sessionId) sessionIdRef.current = outcome.sessionId
        setMsgs((prev) => {
          const next = prev.slice()
          const last = next[next.length - 1]
          if (last && last.role === 'assistant') {
            next[next.length - 1] = {
              ...last,
              streaming: false,
              task: {
                taskId: outcome.taskId,
                status: 'queued',
                phase: '',
                progress: 0,
                step: '',
                reportFiles: [],
                error: '',
              },
            }
          }
          return next
        })
        startPolling()
        // ⚠️ 此处**不调用 saveMessages**：后端已在本次请求内把
        // conversation_history 写进会话，随后还会追加 created/completed/
        // failed 消息。小程序若再 PUT /messages 会整体重写数组，抹掉它们。
      } else {
        // ── 普通问答分支 ──
        let completed = false
        try {
          await streamQuery(text, history, {
            onStatus: (s) => {
              if (completed) return
              if (s && s !== statusRef.current) {
                statusRef.current = s
                restartTimer()
              }
              setStatus(s)
            },
            onToken: (chunk) => {
              if (!completed) {
                setStatus('')
                appendToken(chunk)
              }
            },
            onError: (message) => setError(message),
          })
        } finally {
          completed = true
          finalizeAssistant()
        }
        const assistantMsg: ChatMsg = {
          role: 'assistant',
          content: assistantRef.current,
        }
        await saveMessages(sid, [...history, userMsg, assistantMsg])
      }
```

（删除原有的 `let completed = false` 段落与落库段落——它们已被上面的分支取代。）

在 `copyPatent` 之前加：

```tsx
  function newQueryId(): string {
    return `mini_${Date.now().toString(36)}_${Math.floor(
      Math.random() * 1e6,
    ).toString(36)}`
  }

  async function handleRetryTask(taskId: string) {
    try {
      const newId = await retryTask(taskId)
      setMsgs((prev) =>
        prev.map((m) =>
          m.task && m.task.taskId === taskId
            ? {
                ...m,
                task: {
                  taskId: newId,
                  status: 'queued' as const,
                  phase: '',
                  progress: 0,
                  step: '',
                  reportFiles: [],
                  error: '',
                },
              }
            : m,
        ),
      )
      pollFailRef.current = {}
      startPolling()
    } catch (err) {
      Taro.showToast({ title: errorText(err, '重试失败'), icon: 'none' })
    }
  }
```

渲染区：在助手消息的 patents 块之后（`{m.role === 'assistant' && m.patents ...}` 那块之后）插入：

```tsx
              {m.role === 'assistant' && m.task ? (
                <LongTaskCard
                  task={m.task}
                  onRetry={() => handleRetryTask(m.task!.taskId)}
                  onDownload={(format) => handleDownloadReport(m.task!.taskId, format)}
                />
              ) : null}
```

`handleDownloadReport` 在 Task 10 实现；**本任务先加一个占位**，Task 10 会替换：

```tsx
  function handleDownloadReport(taskId: string, format: string) {
    // Task 10 实现真实下载
    Taro.showToast({ title: `${format} 下载待实现`, icon: 'none' })
  }
```

**页面 scss 不要加 `@import`**——`LongTaskCard/index.tsx` 里的 `import './index.scss'` 已足够（同 Task 4 的说明）。

- [ ] **Step 3: 类型检查 + 构建**

```bash
cd E:/online/workspace/copiioai/langsistance/frontend/weapp && npm run tsc && npm run build:weapp
```

预期：exit 0 + `Compiled successfully`。

- [ ] **Step 4: 人工验证（端到端，需后端可达）**

前置：`API_BASE` 指向测试机（`TARO_APP_API_BASE` 编译期注入），公众平台隐私指引已声明。

1. 开发者工具完全重启
2. 点 `＋` 选一个 PDF → 出现 chip
3. 输入文字 → 发送
4. 预期：用户气泡出现；助手侧出现长任务卡片，显示"正在分析"与百分比；百分比随轮询推进
5. 任务完成后期望卡片变"分析完成"并出现 DOCX / PDF 按钮（此时点击只弹 toast）
6. **关键回归**：切换到别的会话再切回来 → 该轮显示的是**后端写入的**完成态文本，不是卡片。这是预期行为（与 web 一致）。

- [ ] **Step 5: 提交**

```bash
cd E:/online/workspace/copiioai/langsistance && \
  git add frontend/weapp/src/components/LongTaskCard \
          frontend/weapp/src/pages/chat/index.tsx \
          frontend/weapp/src/pages/chat/index.scss && \
  git commit -m "feat(weapp): 长任务卡片与上传分支接入——上传轮不调 saveMessages 以免覆盖后端写入"
```

---

### Task 10: 前端 —— 下载服务

**Files:**
- Create: `frontend/weapp/src/services/download.ts`
- Modify: `frontend/weapp/src/pages/chat/index.tsx`

**Interfaces:**
- Consumes: `clearAuthAndRedirect`（Task 3）
- Produces:
  - `canOpen(format: string): boolean`
  - `saveBase64File(filename: string, chunks: string[]): Promise<string>`
  - `downloadReport(taskId: string, format: string): Promise<string>`
  - `openOrShareFile(filePath: string, format: string): Promise<'opened' | 'shared'>`
  - `exportMarkdown(filename: string, content: string): Promise<string>`
  - `reportDownloadUrl(taskId: string, format: string): string`

**关键约束**：`wx.openDocument` 的 `fileType` 白名单没有 csv，也没有 md；这些走 `shareFileMessage` 转发。

- [ ] **Step 1: 写服务**

创建 `frontend/weapp/src/services/download.ts`：

```ts
import Taro from '@tarojs/taro'
import { API_BASE, STORAGE_KEYS } from '../config'
import { clearAuthAndRedirect } from './api'

/**
 * wx.openDocument 的 fileType 白名单。
 * 官方合法值只有 doc/docx/xls/xlsx/ppt/pptx/pdf——**没有 csv**，
 * 也没有 md。这些格式只能走 shareFileMessage 转发到聊天。
 */
export const OPENABLE_FORMATS = [
  'doc',
  'docx',
  'xls',
  'xlsx',
  'ppt',
  'pptx',
  'pdf',
]

/**
 * 上一次用 writeFile 写进 USER_DATA_PATH 的路径。
 * 该目录有 200MB 上限，反复下载会堆积到写不进去——每次写盘前先删掉上一个，
 * 只保留最新一份。downloadFile 落到系统临时目录，不在此列。
 */
let lastWrittenPath = ''

function cleanupLastWritten(): void {
  if (!lastWrittenPath) return
  try {
    Taro.getFileSystemManager().unlinkSync(lastWrittenPath)
  } catch {
    // 文件可能已被系统回收，忽略
  }
  lastWrittenPath = ''
}

export function canOpen(format: string): boolean {
  return OPENABLE_FORMATS.indexOf((format || '').toLowerCase()) >= 0
}

export function reportDownloadUrl(taskId: string, format: string): string {
  return `${API_BASE}/long_task/${encodeURIComponent(
    taskId,
  )}/report?format=${encodeURIComponent(format)}`
}

function writeFileOnce(
  filePath: string,
  data: string,
  encoding: 'base64' | 'utf8',
): Promise<void> {
  return new Promise<void>((resolve, reject) => {
    Taro.getFileSystemManager().writeFile({
      filePath,
      data,
      encoding,
      success: () => resolve(),
      fail: (err) =>
        reject(new Error((err && err.errMsg) || '文件写入失败')),
    })
  })
}

/** 把 base64 分片写成临时文件，返回文件路径。 */
export async function saveBase64File(
  filename: string,
  chunks: string[],
): Promise<string> {
  cleanupLastWritten()
  const safeName = filename || `CopiioAI_${Date.now()}.bin`
  const filePath = `${Taro.env.USER_DATA_PATH}/${safeName}`
  try {
    // writeFile 接受完整 base64 串，分片在文件系统层拼接
    await writeFileOnce(filePath, chunks.join(''), 'base64')
  } catch (err) {
    // USER_DATA_PATH 有 200MB 上限：可能被别的入口写满了。
    // 再清一次（cleanupLastWritten 只删我们记着的那个），重试一次。
    cleanupLastWritten()
    try {
      await writeFileOnce(filePath, chunks.join(''), 'base64')
    } catch {
      throw err
    }
  }
  lastWrittenPath = filePath
  return filePath
}

/** 下载长任务报告（docx/pdf），返回临时文件路径。 */
export async function downloadReport(
  taskId: string,
  format: string,
): Promise<string> {
  const token = Taro.getStorageSync(STORAGE_KEYS.wxToken)
  const res = await Taro.downloadFile({
    url: reportDownloadUrl(taskId, format),
    header: { Authorization: `Bearer ${token}` },
  })
  if (res.statusCode === 401) clearAuthAndRedirect()
  if (res.statusCode === 404) throw new Error('报告不存在或已过期')
  if (res.statusCode >= 400) throw new Error('报告下载失败，请稍后重试')
  return res.tempFilePath
}

/**
 * 打开；打不开的格式（或打开失败）回退到转发到聊天。
 * 返回实际走的那条路，供调用方给不同提示。
 */
export async function openOrShareFile(
  filePath: string,
  format: string,
): Promise<'opened' | 'shared'> {
  if (canOpen(format)) {
    try {
      await Taro.openDocument({
        filePath,
        fileType: (format || '').toLowerCase(),
        showMenu: true,
      })
      return 'opened'
    } catch {
      // 落到转发——某些机型对 office 格式支持不全
    }
  }
  await Taro.shareFileMessage({ filePath })
  return 'shared'
}

/** 导出消息正文为 Markdown（对齐 web 的 CopiioAI_Chat_<时间戳>.md）。 */
export async function exportMarkdown(
  filename: string,
  content: string,
): Promise<string> {
  cleanupLastWritten()
  const filePath = `${Taro.env.USER_DATA_PATH}/${filename}`
  await writeFileOnce(filePath, content, 'utf8')
  lastWrittenPath = filePath
  return filePath
}
```

- [ ] **Step 2: 替换 Task 9 的占位下载**

`frontend/weapp/src/pages/chat/index.tsx` import 区加：

```tsx
import {
  downloadReport,
  exportMarkdown,
  openOrShareFile,
  saveBase64File,
} from '../../services/download'
```

把 Task 9 留下的占位：

```tsx
  function handleDownloadReport(taskId: string, format: string) {
    // Task 10 实现真实下载
    Taro.showToast({ title: `${format} 下载待实现`, icon: 'none' })
  }
```

替换为：

```tsx
  async function handleDownloadReport(taskId: string, format: string) {
    try {
      Taro.showLoading({ title: '正在下载…' })
      const path = await downloadReport(taskId, format)
      Taro.hideLoading()
      const how = await openOrShareFile(path, format)
      if (how === 'shared') {
        Taro.showToast({ title: '已转发到聊天', icon: 'none' })
      }
    } catch (err) {
      Taro.hideLoading()
      Taro.showToast({ title: errorText(err, '下载失败'), icon: 'none' })
    }
  }
```

- [ ] **Step 3: 类型检查 + 构建**

```bash
cd E:/online/workspace/copiioai/langsistance/frontend/weapp && npm run tsc && npm run build:weapp
```

预期：exit 0 + `Compiled successfully`。

- [ ] **Step 4: 提交**

```bash
cd E:/online/workspace/copiioai/langsistance && \
  git add frontend/weapp/src/services/download.ts \
          frontend/weapp/src/pages/chat/index.tsx && \
  git commit -m "feat(weapp): 下载服务——writeFile/downloadFile + openDocument/shareFileMessage 回退"
```

---

### Task 11: 前端 —— 接上被丢弃的工件帧与下载入口

**Files:**
- Modify: `frontend/weapp/src/services/chatStream.ts`
- Modify: `frontend/weapp/src/pages/chat/index.tsx`
- Modify: `frontend/weapp/src/pages/chat/index.scss`

**Interfaces:**
- Consumes: `CompletedArtifact`（Task 3）、`saveBase64File` / `openOrShareFile` / `exportMarkdown`（Task 10）
- Produces: 无新导出

**背景**：`artifact_*` 帧现在**全被丢弃**——`chatStream.ts:20` 定义了 `onEvent`，但 `pages/chat/index.tsx:274-292` 从没接上，全部落进 `default` 分支（`chatStream.ts:221-222`）。

**硬要求**：`artifact_chunk` **不 setState**。web 的注释（`frontend/nextjs/lib/useChatStream.ts:275-276`）记录，每个 chunk 都重建数组会让前端卡死数分钟——工件是 multi-MB 级（`sources/callback/sse_callback.py:8-10`）。

- [ ] **Step 1: 在 chatStream 里缓冲工件**

`frontend/weapp/src/services/chatStream.ts` —— 在 `streamQuery` 函数体内、`const parser = new SseParser()` 附近加：

```ts
    // 工件缓冲：chunk 只 push 进这里，**不回调页面**——multi-MB 工件每帧
    // 都触发重渲染会把界面卡死（web 在 useChatStream.ts:275-276 有同样结论）。
    // 收齐（artifact_end）后一次性交付。
    const artifactBuf = new Map<
      string,
      { meta: ArtifactPayload; chunks: string[] }
    >()
```

把 `handleEvent(ev, cb, finish)` 的调用改为：

```ts
          handleEvent(ev, cb, finish, artifactBuf)
```

`handleEvent` 签名改为：

```ts
function handleEvent(
  ev: SseEvent,
  cb: StreamCallbacks,
  finish: (err?: Error) => void,
  artifactBuf: Map<string, { meta: ArtifactPayload; chunks: string[] }>,
) {
```

在 `switch` 的 `default:` 之前插入三个分支：

```ts
    case 'artifact_start': {
      // 后端把 metadata 平铺进 start 帧（sse_callback.py:56-59），
      // 字段名是 snake_case
      const id = String(ev.artifact_id || ev.artifactId || '')
      if (!id) break
      artifactBuf.set(id, {
        meta: {
          format: String(ev.format || ''),
          filename: String(ev.filename || ''),
          mimeType: String(ev.mime_type || ev.mimeType || ''),
          rowCount: Number(ev.row_count || 0),
          columnCount: Number(ev.column_count || 0),
        },
        chunks: [],
      })
      break
    }
    case 'artifact_chunk': {
      const id = String(ev.artifact_id || ev.artifactId || '')
      const slot = artifactBuf.get(id)
      if (slot && ev.data) slot.chunks.push(String(ev.data))
      break
    }
    case 'artifact_end': {
      const id = String(ev.artifact_id || ev.artifactId || '')
      const slot = artifactBuf.get(id)
      if (!slot) break
      artifactBuf.delete(id)
      cb.onArtifactsReady?.([{ artifactId: id, ...slot.meta, chunks: slot.chunks }])
      break
    }
```

- [ ] **Step 2: 页面接上**

`frontend/weapp/src/pages/chat/index.tsx`：

import 区加：

```tsx
import { CompletedArtifact } from '../../services/chatStream'
```

`MsgView` 追加：

```tsx
  artifacts?: CompletedArtifact[]
```

**关键**：`finalizeAssistant` 会把消息对象换新，必须保留 `artifacts`；`appendToken` 同理。检查 `appendToken`（`next[next.length-1] = {...last, content: ...}`）与 `finalizeAssistant`（`{...last, streaming:false, patents:...}`）——两处都是展开旧对象，`artifacts` 会自动保留，无需改动。

加回调：

```tsx
  const onArtifactsReady = useCallback((items: CompletedArtifact[]) => {
    if (items.length === 0) return
    setMsgs((prev) => {
      const next = prev.slice()
      const last = next[next.length - 1]
      if (last && last.role === 'assistant') {
        next[next.length - 1] = {
          ...last,
          artifacts: [...(last.artifacts || []), ...items],
        }
      }
      return next
    })
  }, [])
```

在 `streamQuery(text, history, { ... })` 的回调对象里加一行：

```tsx
            onArtifactsReady,
```

加下载与导出处理：

```tsx
  async function handleDownloadArtifact(a: CompletedArtifact) {
    try {
      Taro.showLoading({ title: '正在保存…' })
      const path = await saveBase64File(a.filename, a.chunks)
      Taro.hideLoading()
      const how = await openOrShareFile(path, a.format)
      if (how === 'shared') {
        Taro.showToast({ title: '已转发到聊天', icon: 'none' })
      }
    } catch (err) {
      Taro.hideLoading()
      Taro.showToast({ title: errorText(err, '保存失败'), icon: 'none' })
    }
  }

  async function handleExportMarkdown(m: MsgView) {
    try {
      const ts = new Date()
        .toISOString()
        .replace(/[:.]/g, '-')
        .slice(0, -5)
      const path = await exportMarkdown(`CopiioAI_Chat_${ts}.md`, m.content)
      await openOrShareFile(path, 'md')
      Taro.showToast({ title: '已转发到聊天', icon: 'none' })
    } catch (err) {
      Taro.showToast({ title: errorText(err, '导出失败'), icon: 'none' })
    }
  }
```

渲染：在助手消息的 `<View className='chat-msg-body'>` 闭合之后、patents 块之前插入作品栏。

- `json` 不展示（对齐 web 的 `MarkdownMessage.tsx:239-241`）
- 「导出原文」按钮对齐 web 的 `handleDownload`（`MarkdownMessage.tsx:210-223`）——**这是 spec §10 验收标准 5 的唯一入口，不要省**

```tsx
              {m.role === 'assistant' &&
              (m.artifacts || []).filter((a) => a.format !== 'json').length > 0 ? (
                <View className='chat-msg-artifacts'>
                  {(m.artifacts || [])
                    .filter((a) => a.format !== 'json')
                    .map((a) => (
                      <View
                        key={a.artifactId}
                        className='chat-artifact'
                        onClick={() => handleDownloadArtifact(a)}
                      >
                        <Text className='chat-artifact-badge'>
                          {a.format.toUpperCase()}
                        </Text>
                        <Text className='chat-artifact-label'>
                          {a.format === 'csv' ? '导出 CSV' : '导出 Excel'}
                        </Text>
                      </View>
                    ))}
                </View>
              ) : null}

              {m.role === 'assistant' && m.content ? (
                <View
                  className='chat-artifact chat-artifact-plain'
                  onClick={() => handleExportMarkdown(m)}
                >
                  <Text className='chat-artifact-badge'>MD</Text>
                  <Text className='chat-artifact-label'>导出原文</Text>
                </View>
              ) : null}
```

`frontend/weapp/src/pages/chat/index.scss` 追加：

```scss
/* ── 结果工件（CSV/XLSX 下载）── */
.chat-msg-artifacts {
  display: flex;
  flex-wrap: wrap;
  margin-top: 12px;
  gap: 12px;
}

.chat-artifact {
  display: flex;
  align-items: center;
  padding: 10px 18px;
  border-radius: 12px;
  background: var(--c-primary-soft);
  border: 1px solid rgba(16, 163, 127, 0.25);

  &:active {
    background: #d8ece6;
  }
}

.chat-artifact-badge {
  font-size: 20px;
  font-weight: 600;
  color: var(--c-primary-deep);
}

.chat-artifact-label {
  margin-left: 10px;
  font-size: var(--fs-meta);
  color: var(--c-primary-deep);
}

/* 「导出原文」单独一行，与工件按钮拉开距离 */
.chat-artifact-plain {
  align-self: flex-start;
  margin-top: 12px;
}
```

- [ ] **Step 3: 类型检查 + 构建**

```bash
cd E:/online/workspace/copiioai/langsistance/frontend/weapp && npm run tsc && npm run build:weapp
```

预期：exit 0 + `Compiled successfully`。

- [ ] **Step 4: 人工验证**

开发者工具完全重启后：发一个会产生结果表的提问（如"查一下可折叠桌子相关的专利"）→ 回答下方出现 `导出 Excel` / `导出 CSV` 按钮 → 点 Excel 应能打开预览；点 CSV 应出现转发界面。

- [ ] **Step 5: 提交**

```bash
cd E:/online/workspace/copiioai/langsistance && \
  git add frontend/weapp/src/services/chatStream.ts \
          frontend/weapp/src/pages/chat/index.tsx \
          frontend/weapp/src/pages/chat/index.scss && \
  git commit -m "feat(weapp): 接上此前被丢弃的 artifact 帧——CSV/XLSX 工件可下载导出"
```

---

### Task 12: 前端 —— 分享

**Files:**
- Modify: `frontend/weapp/src/pages/chat/index.tsx`

**Interfaces:**
- Consumes: `sessionTitle`（页面已有 state）
- Produces: 无

**背景**：web 没有可参照的分享功能（`frontend/nextjs/app/app/(auth)/share/page.tsx` 是知识库条目授权共享，与聊天无关）。本轮只做转发卡片。**不做**分享特定会话——会话已有归属校验，转发出去对方只会看到"会话不存在"。

- [ ] **Step 1: 接分享**

`frontend/weapp/src/pages/chat/index.tsx`：

import 区改为：

```tsx
import Taro, { useDidHide, useDidShow, useShareAppMessage, useShareTimeline } from '@tarojs/taro'
```

在组件体内、`useDidShow` 附近加：

```tsx
  // 分享：只做转发卡片，path 指向首页。不做分享特定会话——会话有归属校验，
  // 转发出去对方只会看到「会话不存在」。
  useShareAppMessage(() => ({
    title: sessionTitle ? `CopiioAI：${sessionTitle}` : 'CopiioAI 专利助手',
    path: '/pages/chat/index',
  }))

  useShareTimeline(() => ({
    title: sessionTitle ? `CopiioAI：${sessionTitle}` : 'CopiioAI 专利助手',
    query: '',
  }))

  useEffect(() => {
    Taro.showShareMenu({
      menus: ['shareAppMessage', 'shareTimeline'],
    })
  }, [])
```

- [ ] **Step 2: 类型检查 + 构建**

```bash
cd E:/online/workspace/copiioai/langsistance/frontend/weapp && npm run tsc && npm run build:weapp
```

预期：exit 0 + `Compiled successfully`。

- [ ] **Step 3: 人工验证**

开发者工具完全重启后：点右上角胶囊 → 转发 → 卡片标题应带当前会话标题（或"CopiioAI 专利助手"）。

- [ ] **Step 4: 提交**

```bash
cd E:/online/workspace/copiioai/langsistance && \
  git add frontend/weapp/src/pages/chat/index.tsx && \
  git commit -m "feat(weapp): 转发分享卡片——标题带当前会话"
```

---

### Task 13: web —— 报告下载改 fetch + Blob（修复 Task 1 引入的回归）

**Files:**
- Modify: `frontend/nextjs/services/api.ts`（加 `downloadLongTaskReport`）
- Modify: `frontend/nextjs/components/app/LongTaskProgress.tsx`（`<a>` 换 `<button>`）
- Modify: `frontend/nextjs/styles/components.css`（`.lt-dl-btn` 补 button 复位样式）

**Interfaces:**
- Consumes: `authHeaders()`（`frontend/nextjs/services/api.ts:7-12`）、`getLongTaskReportUrl()`（`frontend/nextjs/services/api.ts:276-278`）
- Produces: `downloadLongTaskReport(taskId: string, format: 'pdf' | 'docx'): Promise<Blob>`

**背景（Task 1 发现的回归）**：web 的报告下载按钮是 `<a href target="_blank">`（`LongTaskProgress.tsx:645-652`），浏览器导航**带不上 `Authorization` 头**。Task 1 给 `/long_task/{id}/report` 加了鉴权后，这个按钮会从"能下"变成 **401**。改用带鉴权的 `fetch` 拿二进制、再本地 Blob 下载——与 web 下载检索工件的方式同构（`MarkdownMessage.tsx:225-237`）。

**执行顺序**：本任务与 Task 1 同批，应在继续 Task 2 之前完成（它修的是 Task 1 引入的回归）。

- [ ] **Step 1: 加下载函数**

`frontend/nextjs/services/api.ts` —— 在 `getLongTaskReportUrl` 之后插入：

```ts
/**
 * 下载长任务报告。必须走 fetch 而非 <a href>：报告端点有归属校验，
 * 需要 Authorization 头，而浏览器导航带不上自定义头。
 */
export async function downloadLongTaskReport(
  taskId: string,
  format: 'pdf' | 'docx' = 'pdf',
): Promise<Blob> {
  const headers = await authHeaders()
  const res = await fetch(getLongTaskReportUrl(taskId, format), { headers })
  if (!res.ok) {
    throw new Error(`报告下载失败(${res.status})`)
  }
  return res.blob()
}
```

- [ ] **Step 2: 组件改用回调下载**

`frontend/nextjs/components/app/LongTaskProgress.tsx`：

import 区加：

```tsx
import { downloadLongTaskReport } from '@/services/api'
```

组件内 state 区（已有 `useState` 导入）加：

```tsx
  const [downloadingLabel, setDownloadingLabel] = useState<string | null>(null)
  const [downloadError, setDownloadError] = useState('')
```

在组件的 return 之前加处理函数：

```tsx
  async function handleReportDownload(label: string, taskId: string) {
    if (downloadingLabel) return
    setDownloadingLabel(label)
    setDownloadError('')
    try {
      const format = label.toLowerCase() === 'docx' ? 'docx' : 'pdf'
      const blob = await downloadLongTaskReport(taskId, format)
      const url = URL.createObjectURL(blob)
      const a = document.createElement('a')
      a.href = url
      // 文件名与后端 Content-Disposition 一致（api_routes/long_task.py:638）
      a.download = `patent_analysis_${taskId}.${format}`
      document.body.appendChild(a)
      a.click()
      document.body.removeChild(a)
      URL.revokeObjectURL(url)
    } catch (err) {
      setDownloadError(err instanceof Error ? err.message : '下载失败，请重试')
    } finally {
      setDownloadingLabel(null)
    }
  }
```

把渲染处（`:645-672` 的 `<a>` 块）整体替换为：

```tsx
      {state.phase === 'completed' && state.reportLinks && (
        <div className="lt-downloads">
          {state.reportLinks.map((link) => (
            <button
              key={link.label}
              type="button"
              className={`lt-dl-btn ${link.label.toLowerCase()}`}
              onClick={() => handleReportDownload(link.label, state.taskId)}
              disabled={downloadingLabel !== null}
            >
              <span className="lt-dl-icon">
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round">
                  <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
                  <polyline points="14 2 14 8 20 8"/>
                  <path d="M12 18v-6"/>
                  <path d="m9 15 3 3 3-3"/>
                </svg>
              </span>
              <span className="lt-dl-label">
                {downloadingLabel === link.label
                  ? t('longTask.downloading')
                  : t('longTask.downloadLabel', { format: link.label })}
              </span>
              <svg className="lt-dl-arrow" viewBox="0 0 16 16" fill="none" strokeWidth="2" strokeLinecap="round">
                <path d="M8 3v8M4 8l4 4 4-4" />
              </svg>
            </button>
          ))}
          {downloadError ? (
            <p className="lt-dl-error">{downloadError}</p>
          ) : null}
        </div>
      )}
```

**i18n**：`t('longTask.downloading')` 需在 `frontend/nextjs/lib/app-i18n/locales/zh.ts` 与 `en.ts` 的 `longTask` 命名空间下各加一条：

```ts
    downloading: '下载中…',      // en.ts: downloading: 'Downloading…',
```

- [ ] **Step 3: 补 button 复位样式**

`<a>` 换 `<button>` 后，UA 默认样式会覆盖两点（`font-family` 不继承、`cursor` 为 default）。在 `frontend/nextjs/styles/components.css:1228` 的 `.lt-dl-btn` 块内补：

```css
  font-family: inherit;
  cursor: pointer;
```

并在该块之后追加禁用态与错误行：

```css
.lt-dl-btn:disabled {
  opacity: 0.6;
  cursor: default;
}

.lt-dl-error {
  flex-basis: 100%;
  margin: 6px 0 0;
  font-size: 13px;
  color: #dc2626;
}
```

- [ ] **Step 4: 构建**

```bash
cd E:/online/workspace/copiioai/langsistance/frontend/nextjs && npm run build
```

预期：构建成功。若仓库无 `build` 脚本，用 `npx tsc --noEmit`。

- [ ] **Step 5: 人工验证**

启动 web 端，打开一个已完成的长任务，点 DOCX / PDF 按钮：
- 预期：按钮变「下载中…」→ 浏览器触发文件下载
- 修复前：新标签页打开并显示 `{"detail":...}` 401

- [ ] **Step 6: 提交**

```bash
cd E:/online/workspace/copiioai/langsistance && \
  git add frontend/nextjs/services/api.ts \
          frontend/nextjs/components/app/LongTaskProgress.tsx \
          frontend/nextjs/styles/components.css \
          frontend/nextjs/lib/app-i18n/locales/zh.ts \
          frontend/nextjs/lib/app-i18n/locales/en.ts && \
  git commit -m "fix(nextjs): 报告下载改 fetch+Blob——<a target=_blank> 带不上 Authorization，加鉴权后会 401"
```

---

## 收尾

- [ ] **跑一遍全量后端回归**（确认 Task 1/2 没打断别的）

```bash
cd E:/online/workspace/copiioai/langsistance && \
  REDIS_HOST=127.0.0.1 REDIS_PORT=6379 PYTHONUTF8=1 \
  python -m pytest tests/test_long_task_api.py tests/test_session_api.py tests/test_task_messages.py -v
```

预期：全部 PASS。

- [ ] **前端最终构建**

```bash
cd E:/online/workspace/copiioai/langsistance/frontend/weapp && npm run tsc && npm run build:weapp
```

- [ ] **真机验证一次**（spec §8 要求，不可省）

开发者工具与真机在文件系统、`openDocument`、`shareFileMessage` 上行为不同。历史教训：此前排版环节所有结论都只在 Windows 开发者工具上看过，真机观感不同。
逐条走 spec §10 的 11 条验收标准。

- [ ] **确认平台配置已做**（Task 5 Step 6）

公众平台已声明「收集你选中的文件」，否则真机上 `chooseMessageFile` 不会唤起。
